# 版本变更记录

本页用于记录各版本发布说明（新增、修复与破坏性变更）。

同一版本的多次功能更新时，应以功能为单位进行更新，比如之前添加了 A 功能的更新，在后续的更新中修复了因 A 功能引入的 bug，那么这个修复说明应该和 A 功能描述放在一起，而不是新增一条修复记录，功能更新同理。

## v0.7.1 (current)

- 新增完整图片设计工作台：提供风格迁移、空间效果适配和跨空间迁移三套严格工作流，复用个人与企业共享素材库，按素材哈希、角色、模型及 schema 版本执行真实视觉分析和缓存；服务端以版本化 PromptPlan 编译、校验并持久化优化提示词，支持人工编辑后重新校验，生成端只接受优化快照 ID，避免浏览器绕过约束。image2 异步任务支持幂等、任务状态查询、结果下载与删除，页面展示工作流规则、分析依据、冲突、硬约束、最终提示词和历史任务；同步扩展 image2 尺寸校验及幂等网络重试，并加入单元契约与固定 30 用例 E2E 质量门禁。

- 修复装修公式词库加载使用旧知识库名称的问题：按“标题词库 / 正文词库”精确读取已索引文件，避免文件已构建却提示必需词库不可用。

- 六个行业的新任务默认入口切换为 Blueprint First v2（`content-workflow-blueprint-first-v2`），支持自动策略选择及报价补证。切换纳入启动初始化：新环境默认启用，已有系统模板的 v3.7 默认绑定自动升级；其他绑定、管理员模板和历史任务保留原配置。目标定义变更时停止切换。新增幂等迁移、历史快照及部署入口检查，覆盖真实登录、六行业新任务版本及页面自动策略提示。

- 内容生产单输入框改为使用独立的最新 `user_request` 数据：提交时清空旧行业表单、品牌、人群和业务变量映射，后端编译及生成上下文不再混入历史字段；移除前端装修测试默认值，并保留视觉素材选择。

- 唯一封面生成完成后自动确认并继续保存，不再要求用户在“选择最终封面”节点重复选择；多封面任务仍保留人工选择，兼容已停留在旧版封面选择节点的任务自动确认。

- 修复知识库使用 DashScope `text-embedding-v4` 时批量超过接口上限导致 Excel 文档入库失败的问题，Milvus 文档与图谱向量请求统一限制为每批最多 10 条；同时修复简化内容表单因提前返回导致的 `compiled` 未初始化 500 错误。

- 内容任务的“一级内容方向”改为按规则库中的“选题类型 → 内容类型”层级选择；没有子分类的选题类型可直接选中，价格营销等包含子分类的方向必须继续选择具体内容类型。行业组合规则的目标范围优先于平台通用内容类型限制，因此装修工长可完整选择自我介绍、价格营销、工艺展示和日常工作，并保存最终内容类型编码按对应规则生成。

- 修复内容运行的模型覆盖链路：创建或重试内容 Run 时指定的 `model_spec` 现在会传递到每个受管内容 Agent，并冻结进节点运行快照，避免子 Agent 意外回落到平台默认模型。

- 装修创作规则升级为工长业务专属模式：保留 9 种正文语义模式和 12 种标题结构，将生产矩阵收敛为与表格逐行对应的 7 组；自我介绍、项目单价、单价+面积、工种总价、人工+辅材、工艺展示和日常工作各自只命中一个手法与正文公式。层级顺序、固定/全取/随机词组及抽取数量写入策略快照，生成与审核 Skill 按同一蓝图执行；项目单价明确不增加证据层，其余报价、工艺和日常证据口径分别锁定。旧版通用 M/T/C 规则按行业隔离，发布沿用行业包同步和历史版本回滚机制。
- HyCanvas 新增两个无需登录、API Key 或工作区参数的公开只读接口：`GET /api/v1/templates/categories` 查询全部模板分类的 ID 和名称，`GET /api/v1/templates/categories/templates?categoryIds=id1,id2` 按多个分类 ID 批量查询模板并按分类分组返回；不存在的分类返回 404，便于外部系统接入“小红书模板专区”等模板分类。

- 新增当家外部内容接入 API（`/api/dangjia/*`）：外部系统 POST 工长人设、施工报价、工地图片的固定 JSON（serialNo 业务流水号作为幂等键），平台自动下载 OBS 图片导入素材库、映射为装修行业事实简报（报价原文进 budget 字段，平台必填的 brand_name/audience/pain 从服务城市与户型面积推导）、编译并自动启动 V3 内容生成，返回 serialNo/task_id/run_id；封面图由唯一非空 templateId 的图片指定，多图按 2/3/4/6/9 组合布局进 photo_composition。鉴权复用平台 API Key（`yxkey_`），另提供任务与 run 查询接口；重复 serialNo 命中幂等返回已有任务，不重复建任务与下载图片。同步修复内容 Worker 在 run 取消/中断收尾时任务已被删除导致的 AttributeError：任务行缺失时仅收尾 run 状态与流事件，不再中断收尾流程。

- 修复推理模型执行内容生成时将隐藏 reasoning Token 计入节点可见生成预算的问题；内容节点继续严格限制可见输出 Token，避免正文长度正常却误报“Token 使用超过节点预算”。

- 联合策略初选与复评新增 JointStrategyPromptV1 模型视图：完整输入先校验和审计，模型仅接收决策事实、完整候选与规则、任务锁定版本的渠道/人设语义。移出视觉及审计配置，仅合并同源同值的简报副本，保留证据数组位置、引用路径、用途和价格适用范围；原创移除参考候选和仿写专属 Skill，仿写保留参考卡、结构摘要和必要槽位。联合选择 Skill 按模式注入并记录版本及生效哈希，输出仍按完整原始事实和规则校验。工具输入体积改为测量完整 SDK 工具定义，避免漏算嵌套契约。

- 将有限调用恢复覆盖创作策略初选与复评，修复首轮校验失败后被 75 秒总时限中断的问题：策略初选/复评与正文生成改为按真实流式输出进展续期，策略 65 秒、正文 120 秒仅限制连续无输出时长；外层 Agent 节点同步续期，不再按固定总时长取消仍在输出的模型。空心跳不续期，停流会取消并清理请求，用户取消及两次调用/输出额度仍有效；快照记录 idle 超时语义。记录真实输出进展并关闭 SDK 叠加重试。同分主手法校验返回明确的正确候选，保持有依据的评分不变，更新对应 Skill。

- 正文生成增加独立版本的模型输入视图，完整审计快照继续保留；新任务合并同源表单别名事实并排除配置字段。原创按模式收窄 Skill，避免图构建重复展开权限与指令，统一动态排版规则与一次自检。正文默认显式使用 medium 推理，单次节点执行的模型调用、连接恢复与格式纠正共用两次调用额度，节点看门狗按输出进展续期（连续无进展上限 300 秒），并记录输入分项、首个输出进展、最终配置和失败阶段。证据校验一次反馈全部错误引用位置，修复“第一次”等序数表达触发排名宣传校验的问题。

- 修复 HyCanvas 模板选择“所有人”后无法在工作区模板分类中显示的问题：公开范围只控制模板的使用权限，继续保留模板分类所属的来源工作区；工作区分类查询同时兼容历史上已清空工作区但仍绑定分类的公开模板。

- 素材图片图库支持个人与企业共享：本站所有登录成员可使用共享图片，不按部门隔离；管理员管理共享图库，上传者管理自己的素材。支持父子图库范围继承、旧图库转共享及跨图库移动，统一接入 HyCanvas 素材选择和内容封面生成。共享图片下架保留已授权作品与任务的原图引用，操作留痕，已有素材默认保持私有。同步修正 HyCanvas 首页可选路由参数的 TypeScript 类型，恢复生产构建。

- 修复生成结果“爆款原文”接口未适配参考资产工作流而返回 500：按已选资产版本读取完整标题和正文，保留知识库访问权限校验；兼容旧工作流原文记录，记录缺失时明确返回 404。

- 修复联合策略校验失败后丢失上一轮提交内容、迫使模型重新生成整份结果的问题：保留最近一次被拒结果及校验提示用于修正；提交契约明确原创模式也必须包含 reference，在可见证据旁标注准确引用路径以避免模型自行计数出错，不改冻结证据，继续执行原有结构与业务校验。

- 修复 HyCanvas 模板实例化后文字样式与模板协议不一致的问题：生成时会按模板字段协议恢复完整的富文本 run 样式、段落样式、文本框配置和文字效果，覆盖字体族、字形与字重、字号、颜色、字距、行高、大小写、装饰、基线、上下标、段落对齐、缩进、自动适配、内边距、垂直对齐和阴影等属性；兼容历史模板已有的精简 typography 协议，并统一服务端 PNG、SVG、PDF 对命名字重的解析，以及 SVG、PDF 对斜体的解析，避免替换文案后出现粗体、斜体等样式失效。

- 修复内容生产工作流中“内容生成公式”始终挂在动态输出底部的问题：公式首次生成后固定在当时的叙述位置，后续工作流信息继续显示在其下方，“正在思考”保持为当前末尾状态。

- HyCanvas 模板支持工作区分类：保存编辑器模板、导入 PPTX 模板时必须选择分类，并可在弹窗内新建分类；模板页面在原“全部”筛选位置直接展示各分类并按分类筛选，移除重复的分类选择区域，选择具体分类时仅展示该分类模板、不再混入普通设计稿，便于建立“小红书模板专区”等模板专区。新增无需认证和工作区参数的公开只读接口 `GET /api/v1/templates/catalog`，返回系统内“多个分类、每个分类包含多个模板”的分组数据。保存并设置模板时设计稿改为静默落盘，仅在模板真正创建成功后显示模板保存提示，避免提前出现“已保存”造成误解。

- 图库封面支持单图与九种图片组合布局，图库弹窗可一次多选最多 9 张图片并按选择顺序自动建立组合，组合中的“插入图片”入口也可一次选择多张并从当前位置起依次填满空位；可设置首图、跨图库填图、拖动换位和调整裁切焦点，组合随草稿保存。生成结果详情可通过左右箭头查看正式封面与一张组合预览图，组合预览固定每行展示两张图片并占满左侧可用宽度，多余图片继续换行排列，内容超出预览区域时可滚动查看；模板叠加预览与正式 HyCanvas 封面始终以选定的封面原图为底图，不再先把多张组合图片拼接后叠加模板。修复组合预览因图标组件与浏览器图片对象命名冲突而无法显示，以及文本形式的受众输入导致草稿保存失败的问题。

- 修复生成结果列表的历史版本封面显示：历史版本按其保存的 `cover_asset_id` 加载并展示对应封面，不再因结果卡片只绑定当前版本封面而显示“暂无封面”。


- 优化内容生成完成后的 AI 修改交互：发送指令后立即在对话区显示用户消息及执行状态，输入框同步显示“正在执行修改”并禁用重复提交；失败时保留错误反馈并恢复原指令，避免仅显示含义不明的暂停图标。

- 内容新建页移除使用模式切换，默认以专业版创建任务；原创模式与爆款仿写改为两个紧凑选择块，选中时显示淡蓝色背景。
- 修复专业版业务简报被旧表单空渠道值误判为缺少必填字段：以任务绑定的发布渠道校验，表单显示已绑定渠道，并在校验失败时列出具体缺失字段。

- 取消封面视觉审核：生成成功后直接预览、选择并确认保存，不再调用审核 Agent 或以历史审核结果阻断；保留封面任务归属、输出资产及生成成功校验。历史等待中的任务自动展示实际生成的封面，图片保持完整比例。

- Emoji 表达改为按正文实际语义覆盖：痛点与情感、人设判断、数据类别、事项列表、提醒和互动各自使用恰当符号，不再受全篇 2～5 个或参考样例密度限制。同步修正仿写、排版、去机械腔和严格审核 Skill，保留原有事实、列表结构及渠道禁用约束；仍在原生成调用内完成，不增加模型节点。

- 修复流式输出将正文换行压缩成空格的问题：“正文内容”独立成行，保留原有自然段、列表和 Markdown 排版，展示完整正文，不再套用摘要截断限制。

- 内容生产页将账号管理、图片识别、生产历史和创作规则库集中到输入框内部右上方工具栏，与输入框共用一个完整边框、不设分隔线，移除顶部返回按钮；工具栏随输入区保留，窄屏自动换行，规则库权限与图片识别启用条件保持一致。生产内容区取消居中宽度和输出框固定高度限制，左右外边距统一采用页面间距，结果栏保持原有约 394px 宽度，左侧输入与输出区自适应扩展；长内容随整页滚动，顶部标题可滚出视口，输入栏保留在底部，手动上翻阅读时停止自动追随新输出。

- 知识库上传新增“用于爆款仿写参考”选项，文件解析成功后异步触发文章识别及参考卡、结构蓝图准备，不依赖向量入库完成。新增正式文章识别 Skill，Agent 选择原文单元，程序保留来源和正文顺序，XLSX/CSV 从原始单元格提取并保留换行；按知识库行业配置或文案内容确定行业，纯报价表和不完整内容明确返回问题。已有文件可多选自动准备，取消日常手填布局、列名及认定依据的登记流程；用户指定参考用途不冒充平台传播效果。规则库展示文件准备进度、文章可用数量、可读参考卡、九项蓝图及原文依据，保留权限、幂等、版本失效和重试记录。修复爆款参考资产的行业筛选将模板历史版本重复展示为行业的问题，按行业标识去重，模板历史保留。

- 内容创作新增独立 Agent + Skill 工作流：装修仅在用户选择的一级内容方向内匹配标题与正文公式，其他行业按当前输入评分，所有行业逐项评价创作手法；规则权重与同分规则保存在正式 Skill 资源，代码校验候选、证据路径和计分并锁定版本。爆款参考按完整文章版本提前异步准备原文快照、引用锚点、参考卡和结构蓝图，在线只比较有限卡片并读取选中蓝图；新增资产管理、原文查看、分项评分及失败恢复展示。新旧工作流与历史契约隔离，提供显式切换和回滚脚本，启动时保留管理员默认版本绑定。本地六行业已切换新入口，云端未发布；修复受管模型未传递推理强度配置及恢复运行无法展示原策略结果的问题。联合选择 Skill 明确缺失资料只能写入淘汰理由，不能作为评分证据路径；结果契约补充路径约束说明，保留不存在或空字段的严格校验。执行详情直接展示业务校验原因，避免只显示异常类型。选择依据仅展示已选中的标题公式、正文公式和创作手法，保留中文名称、评分、理由与中文输入依据，不再列出未选候选及参考评分行；选中参考的完整原文仍可单独查看。 新增蓝图优先工作流：新任务无需选择内容方向，选择 Agent 在同一次调用内结合真实资料与已准备蓝图卡确定参考、内部方向、公式和手法；装修仅在自动采用的方向内匹配公式，其他行业继续评分。检索覆盖结构摘要，结果展示自动采用方向与原因，不增加独立资料判断调用。

- 创作规则库按飞书原文补全7个标题公式、4个正文公式及28个装修组合的场景、变量、结构和案例，支持管理员草稿编辑、启停与删除。组合显示中文关系并按行业筛选；停用公式在运行时退出候选池；修复创建草稿的数据库锁查询及新任务预览固定读取旧规则版本的问题，发布保留历史任务版本。
- 创作矩阵支持全行业：餐饮、教育、美业、零售、专业服务各7个方向、14组独立编排，保留装修原文28组；共享公式的通用应用说明可编辑，按任务行业进入生成快照和Agent规则工具，清理旧版迁移占位组合。发布规则时在同一事务内同步行业包的组合引用、方向别名和结构校验样本，沿用原业务策略；页面显示当前6个行业包及绑定规则，历史版本可展开。历史任务保持原行业包及规则版本，新版装修包继续执行必需词库检查；结构同步不冒充完整生成回归。

- 封面视觉规划按模板中的每个叙事文字框分别生成文案，同一张封面内主标题、副标题和摘要不得重复同一句或仅换序复述同一组对象与数字；契约层会在忽略空格、标点和换行后拦截完全重复内容并要求 Agent 定点改写。字段填充优先使用模板唯一 `key` 并兼容旧模板名称，即使多个图层原文名称相同也能分别接收不同文案；HyCanvas 实例化不再让同名图层互相覆盖，同时仍只替换文字值，不改变模板结构、样式和标签原文。

- 原创内容生成在同一次 Agent 调用内强制加载中文自然化、语义化 Emoji 与正文排版 Skills：先按公式和证据生成，再拆分场景、数据、方案与互动信息块，去除机械报幕和模板腔，并在小红书等允许 Emoji 的渠道将少量表情分散放到真实语义锚点；不新增二次大模型润色调用，事实、数字、Evidence 和段落证据保持不变。

- 优化素材库图库卡片：缩略图使用稳定的 16:9 比例占据卡片主体，说明文字压缩为单行省略，收紧信息区与操作按钮间距，避免文字区挤占图片展示空间。

- 内容任务详情页标题区新增“返回上一页”按钮；仅当 Vue 路由状态记录了有效前页时执行后退，从空白页直接打开或没有站内前页时回到生产历史，避免返回 `about:blank`；新建内容任务首页不显示该按钮。

- 生成历史支持跨页多选与批量删除：服务端在同一事务中完成任务归属校验后统一软删除，旧版只读任务也允许清理，任一任务不可访问时整批不落库；前端提供选中数量、全选、删除确认、处理状态和分页回退，完成后立即刷新历史列表。

- 新增生产环境增量热发布通道：根据 Git 差异自动选择 API/Worker/Skill、Web 或 HyCanvas，仅在当前线上镜像之上生成小体积源码/静态资源增量层；API 组件采用 Worker 停机等待与服务滚动重建，Web 清理后替换完整构建产物，HyCanvas 复用单文件快速发布。依赖锁、数据库迁移、Compose/Dockerfile 和运行基础设施变化会被阻断并要求完整不可变镜像发布；增量发布记录前后镜像引用，健康检查失败自动回滚，并提供最近一次 API/Web 人工回滚脚本。

- 优化内容生产的图库图片选择弹层：图片列表元数据返回后立即展示可选网格，缩略图按四路并发渐进加载，不再等待 20–100 张原始大图全部下载后才结束加载；新增 480px JPEG 缩略图接口并提供一天浏览器缓存，选择确认和最终生成仍使用无损原图。

- 内容生产工作台在确认图库主图后，会并排展示封面原图与“图库图片作为底图、所选 HyCanvas 大字报模板保留文字和装饰层”的合成缩略预览；合成预览复用正式实例化的背景替换和服务端渲染逻辑，但只在内存中生成、不创建设计稿或封面资产。刷新恢复任务、更换图片、切换模板和清除选择时预览会同步更新，移动端自动换行以保留操作空间，两张缩略图均支持点击放大查看。修复生成结果进入 HyCanvas 编辑器后只显示模板、不显示封面原图的问题：新设计将原图写入标准资产引用，历史设计在加载时自动迁移；旧节点未记录原始像素尺寸时，会按浏览器实际解码尺寸执行 `cover` 裁切。

- 新增 HyCanvas 单文件快速发布通道：在本地完成生产前端构建和 Linux AMD64 静态编译，通过 SSH 只传输压缩后的单个可执行文件；服务器校验 SHA256 后使用版本目录和软链接原子切换，仅重建 `hycanvas-app`。新版本 60 秒内未通过健康检查时自动恢复并启动上一版本，同时提供独立的人工回滚脚本；完整不可变镜像链路继续用于首次部署、基础镜像升级和全量发布。

- 优化 HyCanvas 模板专区与设计列表加载性能：保存设计和模板时生成小尺寸持久化缩略图，列表优先读取缩略图而不再下载完整画布；历史设计缺少缩略图时限流渲染真实预览并自动回填，预览接口剥离可能达到数百 MB 的嵌入字体并使用系统字体完成一次性渲染，避免长期显示占位色块，后续访问直接读取小图；页面切换会取消尚未开始的旧预览任务，全局兼容渲染最多并发两项。模板列表只预取可视区域附近的预览，点击模板时立即取消排队及正在下载的其他模板文件，避免第二次打开被后台预览请求阻塞。设计文件读写同时对内嵌字体总量设置异常保护：普通单个自定义字体继续随文档保存，只有字体集合超过 32 MB 的历史导入文件会剥离 Base64 字节并保留字体族引用；浏览器在保存设计或模板前执行同样的瘦身，不再先序列化和上传服务器必然丢弃的数百 MB 字体数据，服务端模板入口对其他 API 客户端执行相同约束。数据库迁移会一次性修复历史超大模板，避免模板目录为了读取尺寸和字段摘要而反复解压、解析数百 MB JSON。旧版本设计快照仍可恢复，当前文件在下一次保存后从约 180–230 MB 缩减为正常大小，避免打开或保存时传输和解析数百 MB JSON。

- 生产镜像发布与部署链路纳入 HyCanvas：GitHub Actions 与 API、Web、Sandbox Provisioner 同批构建并提升 HyCanvas 不可变镜像，digest 清单和服务器部署脚本统一校验、拉取四类镜像，避免主站与封面编辑器版本不一致；内置 Skills 继续随 API 镜像发布。CI 集成测试依赖改为动态宿主机端口，避免共享 Runner 端口占用导致发布误失败；HyCanvas 构建时将版本号规范化为 Next.js 合法的 deployment ID，不再因语义版本中的点号中断发布。

- ContentSwarm 本地 Compose 为 HyCanvas 增加完整热更新环境：开发容器挂载源码，Next.js 前端与 Go API 分端口运行，ContentFlow 浏览器链接自动指向开发前端；Go 源码由 air 自动重启，前端由 Next.js 热更新，共享包和模板定义由轮询监听自动编译，模板 seed 变化会触发后端重启。本机 ContentSwarm 来源可连接 HyCanvas 热更新通道，避免跨端口嵌入时退化为整个 iframe 重新加载、丢失弹窗中的未保存设置；HyCanvas 浏览器 API 统一走 Next.js 同源代理，避免 `localhost` 与 `127.0.0.1` 不同主机导致认证 Cookie 丢失、工作台持续加载，并修复 Next.js 开发服务未生成 `/dashboard` 可选 catch-all 根路径导致票据兑换后落入 404 的问题。依赖卷按 lockfile 缓存，普通容器重建不再重复安装依赖；生产 Compose 继续使用前端与模板内嵌的单二进制镜像。托管素材桥接以 iframe 的真实父页面来源为准，即使用户通过 IP、备用域名或另一层反向代理访问，也不会因构建时配置的规范域名不同而被浏览器丢弃消息并等待到超时。

- 移除 HyCanvas 模板库中 9 个小红书系统预置模板；用户自建模板、工作区模板及素材库图片不受影响。

- HyCanvas 自定义模板支持删除：模板卡片更多菜单新增删除入口和不可撤销确认，自建个人模板仅创建者可删除，工作区模板沿用成员权限，系统内置模板保持只读；删除成功后模板列表即时更新，接口失败会保留模板并提示错误；同时修复模板列表在排序后丢失工作区标识、导致工作区模板管理菜单被错误隐藏的问题。

- 内容工作流升级为 V3.7，修复爆款仿写错误套用固定结构、回修重复提交以及单一调研 Agent 高频超时的问题：原调研节点拆为业务与平台规则、价格、封禁词、爆款候选四个独立 Agent 并发执行，每个节点使用独立 Skill、最小输入输出契约、知识库范围、独立节点预算和运行记录；知识检索同时限制片段数量与单片段字符数，爆款候选最多返回两篇且单篇原文不超过 800 字，专职 Agent 在完成许可检索后立即提交且不执行模型错误重试，避免长爆款原文和结构化回修再次耗尽节点预算。价格、业务规则、封禁词和爆款候选调研分别按各自单次模型调用上限设置节点总时限，为“检索决策、知识库查询、结构化提交”两轮调用预留完整预算和调度开销。四路结果汇合后，爆款匹配 Agent 只根据当前任务非空变量和候选原文比较匹配度，业务、价格与合规证据留给后续确定性汇总和生成阶段使用；无法承接当前变量的清单、结果或结算结构会被淘汰。冻结蓝图新增标题槽位、正文信息块和真实列表类型，生成与审核均按选中爆款动态执行，不再强制所有内容使用 `1–4` 编号、固定五段或四个空行。回修时生成 Agent 可读取上一轮标题、正文及语义审核意见，标题不得把预算、预计工期或方案结果扩大成已完工、已结算事实，也不得原样重复提交。行首顺序编号仅作为蓝图要求下的结构导航，不再被误判为无证据事实数字。原创模式跳过爆款候选与选择模型调用；并发状态使用可合并的 Agent Run 映射，页面分别展示四路调研、爆款选择和证据汇总进度。真实任务验收中四路调研在 20 毫秒内同时启动，并发阶段约 34 秒完成；同时修正视觉素材前后规则不一致的问题，选择 HyCanvas 模板时必须先选择图库主图，错误会在编译简报时直接提示，不再等到内容完成后的封面节点才失败。

  首稿链路进一步去除不必要的串行等待：策略组合与标题/正文公式改由既有固定规则按优先级、变量和证据覆盖度确定性锁定，不再单独调用策略 Agent；普通首稿通过确定性校验后默认跳过语义大模型复读，仅显式严格审核时调用。简报没有证据缺口时，业务规则、价格与合规调研直接返回，不再创建无收益的子 Agent Run；需要主动扩充资料时仍可显式强制调研。知识库新增但未经用户确认的价格、优惠、效果承诺等高风险资料会自动退出本次冻结证据并记录为待确认项，用户在简报中已确认的事实继续正常使用，因此生成前不再出现无必要的人工门禁。固定校验不通过时按唯一目标自动定点回修，通过后自动批准文案，不再等待人工点击；最大回修次数和封面人工选择仍保留。并行调研节点按工作流定义和节点输入生成稳定快照，成功结果随节点 Run 持久化；失败重试时输入未变化的成功分支直接复用，只重新执行失败或输入已变化的节点。视觉规划 Agent 读取模板非标签字段的字数与行数上限，提交超限时会收到字段级错误并在当前节点内进行语义压缩：保留核心对象、关键事实、数字和表达意图，优先删除修饰与重复信息，禁止机械截断或产生残句；多行字段按 `maxCharsPerLine` 和 `maxLines` 在完整词组边界换行，避免总字数合规但横向溢出。模板必填字段缺少对应事实时，视觉规划 Agent 会在首轮规划中收到待适配字段及长度约束，依据已审批内容生成中性短句；“免费、保证、省钱”等无证据承诺不会沿用，只有确实无法形成合规文案时才阻断。旧运行若已在封面提交节点因超限或缺失事实失败，重试会自动回退到视觉规划节点生成合规短文案，不再重复提交旧方案或把模板字段异常留给用户处理。HyCanvas 模板实例化只替换声明字段的文字值，保持原节点、段落、文字样式、文本框和自动适配配置不变；文字放不下时由视觉规划 Agent 缩短，不再修改字号或版式容纳长文案。修复 HyCanvas 渲染完成后仍被通用封面归一化流程二次叠加白色圆角标题板和红色竖线的问题，模板封面最终图片不再添加任何模板外标题图层。

- 修复内容策略 Agent 偶发等待满 120 秒后直接失败的问题：为策略模型的单次调用增加 70 秒硬超时，并在节点总预算内自动重试一次；上游模型请求偶发挂起时不再耗尽整个节点时限，运行快照同时记录单次模型超时与重试配置，便于后续定位。

- 修复并行调研汇合点被 Worker 重载中断后，自动重试因缺少节点归属而报 `Ambiguous update, specify as_node` 的问题：当 checkpoint 只有一个待执行汇合节点时，从工作流边反推其已完成前驱并显式更新 checkpoint，继续待执行节点且不重跑已完成的并行调研。

- 修复爆款仿写正文的 Emoji 全部被机械追加在段末的问题：爆款参考蓝图必须提取每个可观察表情的结构块、相对位置、语义锨点和功能；结构仿写、排版与人味表达 Skill 均要求叙事分散型 Emoji 紧贴场景、金额、转折、确认或互动语义，不得全部位于段首或段末；审核 Skill 同步将此类机械落点判定为人设风格不匹配。

- 爆款仿写的生成结果详情在“封面 / 编辑”后新增“爆款原文”入口，点击后直接在左侧预览区切换 Tab，不再打开二层弹框；原文视图展示本次实际选中的候选原文、文档来源和知识库名称，并支持复制。原文仅在用户点击时从已完成的爆款检索节点读取，不重新放入生成 Agent 的冻结证据，避免模型复制原文事实；原创模式不显示该入口。

- 生成结果详情保持原有弹窗宽度和左右栏比例，增加可用纵向高度；同时压缩标题与发布标签的间距和内边距，发布标签卡片进一步减少标题行高、标签行距和上下留白，将剩余空间优先分配给正文文案，中等长度文章可尽量一屏完整显示，仅在超出屏幕容量时使用正文内部滚动。

- 内容生成新增由 Skill 驱动的封禁词替换闭环：内容调研 Skill 会从授权“封禁词库”读取“问题词—常用表达方式”表并冻结为平台规则 Evidence，正文表达 Skill 根据上下文从表内候选中选择自然替换，内容审核 Skill 逐项阻断遗漏或语义错误；具体问题词和替换表达不写入程序代码，知识库表格更新后即可用于后续新任务，空白候选不会被误删或凭空补词。调研节点同步支持价格、品牌、平台规则、爆款与封禁词五类知识库，避免新增封禁词库后因四轮检索上限无法提交结果。

- 素材图片图库新增行业归属：每个一级图库唯一绑定一个已发布行业，二级图库和图片继承父图库行业；素材库首页按行业分组并支持行业筛选，历史图库统一显示在“未分类行业”。内容生产选择行业后加载同一行业及“未分类”通用图库的一级、二级图库，继续保留原有两级图库结构，避免历史素材因未绑定行业而不可见。

- HyCanvas 可编辑封面模板新增一期结构化内容填充：模板作者在另存模板时可从文字图层声明可填字段、语义角色、稳定编码、必填和最大字数，小字号的话题、品类和辅助短文案可标记为“标签（保留原文）”，实例化时不替换、不参与必填校验；ContentSwarm 托管编辑器中每次点击“保存”，都会在设计快照保存成功后弹出模板字段协议确认窗，并带出已有字段供确认或修改；字段保存后写回设计元数据。字段声明随模板设计快照保存并由模板目录返回。内容封面任务按 AI 已生成的锁定视觉文案和事实简报精确映射标题、项目名称、面积、设计师、完成年份及品牌等可替换字段，不再只按“标题/副标题”名称猜测；实例化前后端共同拦截缺少必填事实和超长文案，生成后的 HyCanvas 文字仍保持原位置、样式和可编辑能力。一期不包含图片 OCR 和自动拆层。

- 修复 HyCanvas 生产发布后首页可用但编辑器持续加载的问题：为 HTTP 私有化部署补充基于 `crypto.getRandomValues` 的 UUID v4 兼容实现，编辑器入口改为同包加载，生产静态导出为每个版本注入独立部署 ID。服务端为 `/_next/static/chunks/` 生成内容 ETag 并进行条件校验，发布后内容变化会自动下载新版本，未变化的多兆字节脚本则返回 304 并复用本地缓存，避免新旧运行时混用的同时提升重复进入工作台的速度。

- 开发与生产 Docker Compose 均纳入 ContentSwarm 托管模式的 HyCanvas 应用、独立 PostgreSQL 与幂等初始化任务；本地启动会自动创建集成用户、固定工作区和具备生成/读取/导出权限的 API Key，并向 API/Worker 注入服务地址、浏览器地址、Key 与工作区配置，使新环境拉取代码后无需手工配置即可使用视觉创作。生产环境强制显式配置独立数据库密码、JWT 密钥、API Key、工作区及公开访问地址。

- 将现有上传/远程安装的 18 个通用 Skill 固化为平台内置能力，完整纳入源码、脚本、模板、字体和许可证；启动同步会把数据库中的同名运行时记录原位升级为 `builtin`，保留记录 ID 与启用状态，并统一使用全局共享权限，确保新环境从 Git 部署后自动具备相同能力。

- 内容审核阶段接入独立部署的 HyCanvas 可编辑视觉服务：后端使用工作区 API Key 代理小红书模板列表、按声明字段创建设计稿和高清 PNG 渲染，浏览器不接触集成密钥；ContentFlow 新增“小红书模板专区”，可根据当前标题、正文和品牌信息预填模板字段，支持替换模板主图，人工校对后一键创建并进入 HyCanvas 继续编辑。设计稿快照永久写入内容版本，高清 PNG 同步进入封面资产库并自动成为发布封面，刷新审核页面可恢复设计稿。结果详情的“编辑”改为进入 ContentFlow 内嵌的设计工作台，不再打开独立网页；侧边菜单将独立“封面生成”“设计工作台”“素材库”收敛为一个“视觉创作”入口，页面顶部通过“设计工作台 / 素材库”切换。素材库只管理素材图片，旧“封面模板”页签和地址下线，模板统一进入设计工作台管理。两种入口均由服务端 API Key 换取一分钟短期票据，编辑票据绑定 design ID，工作区票据绑定 workspace ID，ContentFlow 地址栏不保留票据。嵌入编辑器保存后通过受限来源消息返回内容结果页，由服务端重新渲染 PNG、创建新封面资产和内容版本，并自动恢复结果详情弹窗。HyCanvas 新增 ContentSwarm 托管认证模式，关闭独立密码、Magic Link 与 OIDC 登录，登录、注册和密码找回页面统一返回 ContentFlow；新会话只能由 ContentFlow 短期票据建立，原有 HttpOnly 会话、刷新和工作区权限边界继续保留。本地内嵌会话会统一 ContentSwarm 与 HyCanvas 的 `localhost` / loopback IP 主机名，避免 `SameSite=Lax` Cookie 因主机名混用而丢失；视觉稿创建与“继续编辑”入口也统一先兑换短期票据，不再直连未认证编辑器。托管工作台隐藏 HyCanvas 的第二套 Logo、工作区切换、侧栏、账户菜单及邮箱验证提醒，改用轻量顶部功能导航，并统一为 ContentSwarm 青蓝色视觉体系，避免内嵌后呈现两套系统。托管编辑器的“素材库”通过受限父页面消息读取 ContentSwarm 当前用户的一、二级图库、真实图片和搜索结果，缩略图与点击插入均由 ContentSwarm 代为鉴权读取，不向 HyCanvas 暴露登录令牌；原 HyCanvas 默认素材统一归入“系统素材”，继续提供浏览、收藏、最近及照片、插画、图标、表情分类，ContentSwarm 图库则归入“我的素材”。托管上传面板直接展示 ContentSwarm 素材图库，点击分类后在“你的媒体”中读取、搜索、预览并插入真实素材；新建文件夹时同步创建图片图库，图片上传成功后同步写入当前图库，未选择文件夹时进入“未分类”。HyCanvas 原上传记录与其他导入能力继续保留。原先依赖 Lorem Picsum 的系统示例照片改为本地确定性素材，保留原 ID、分类、收藏和最近记录，预览与画布插入均不再依赖外网。HyCanvas 独立模式继续使用原素材库。右上角“创建”菜单选择“小红书模板专区”时会创建 1080×1440 模板草稿并进入编辑器，草稿在“保存为模板”时自动携带“小红书”分类与标签；专区与 ContentFlow 模板代理统一按“小红书”标签识别模板，使工作区自定义模板可在刷新后继续显示。HyCanvas 完整源码纳入 ContentSwarm 单仓库的 `apps/hycanvas` 模块统一维护和提交，运行时仍保持独立服务边界，并通过 `HYCANVAS_BASE_URL`、`HYCANVAS_PUBLIC_URL`、`HYCANVAS_API_KEY` 和 `HYCANVAS_WORKSPACE_ID` 配置联调；原 HyCanvas Git 仓库仅作为上游参考，不再作为平台功能的发布目标。

  小红书专区进一步同时展示带专区归属的设计稿和带“小红书”标签的可复用模板，历史同名草稿会自动回填归属，不再出现首页有设计而专区为空的情况。历史草稿“保存为模板”时改由后端根据设计摘要强制继承小红书标签，已保存但缺标签的模板会在迁移中原地修复。自定义模板新增独立重命名入口，标题在原模板 ID 上更新，不再通过“保存为模板”误建副本；应用带小红书标签的模板时，新设计会继续继承专区归属。代理列表不再要求历史自定义模板必须声明可填字段，避免合并后已保存模板被错误隐藏。

- 装修行业标题与正文生成接入飞书“3.1 标题调用 / 3.2 正文调用”执行契约：锁定 T01～T07、C01～C04 后新增固定词库加载节点，按公式从“标题资料库”和“正文资料库”精确加载对应已索引文件，缺库、文件不可用或内容为空时直接阻断；行业识别统一读取已冻结运行快照，避免节点输入没有顶层行业字段时被误判为非装修，生成输入契约也会拒绝将装修公式词库包降级为 `required=false`。生成结果必须回报实际使用的词库编码和原样词条，词库只作为表达来源，事实、数字、案例、身份和承诺仍受 EvidenceBundle 约束。

- 素材库的素材图片支持两级图库：可在普通一级图库中创建二级图库，并在两级图库中直接上传、移动和管理图片；一级图库卡片汇总自身与二级图库的图片数量，内容生产页外层只展示一级图库，二级图库收纳在所属图库内，其他图片选择器也按一级图库分组展示子图库。系统“未分类”图库和封面模板分类保持单层，后端拒绝第三级图库，并阻止直接删除仍包含二级图库的一级图库。

- 内容结果封面新增原生画板编辑闭环：结果详情在封面标题处提供“封面 / 编辑”入口，进入独立全屏编辑器后可恢复大字报的干净底图与全部蒙版文字图层，包括固定品牌字、中部说明和页脚；鸿扬长版模板按本人 `tuxianghuaban` 仓库的回归标准校准为 8 个真实逻辑图层，合并面积上标与字距较大的页脚，修正 OCR 碎片和英文误识，图层直接使用真实文案命名而不展示“口号/眉题/副标题”等猜测角色，并恢复 Georgia、SimSun、Arial、字号、字重、字距、阴影与精确坐标。应用后再次打开派生封面时继承原场景，旧 OCR 碎片自动移除且用户新增图层保留，单行长文案按原框等比缩小避免截断。画板支持新增、复制、删除、隐藏、锁定、排序、拖动、缩放、撤销重做以及字体、字号、字重、颜色、描边、阴影、底色、透明度、行高、字距、对齐和旋转调整；方向键支持 1px 微调，Shift 组合键支持 10px 微调。历史扁平封面保持锁定底图并明确提示原图文字不可直接拆层。编辑草稿以 revision 乐观锁自动保存，服务端通过新的 `editor_render` Worker 任务确定性输出 PNG；应用结果继续复用当前封面切换与内容版本链路，不覆盖原封面或既有发布快照，渲染失败时保留草稿。不透明原图模板识别出文字后改用“产品图全画布底图、文字与装饰逐层叠加”模式，无需再人工框选局部产品区；OCR 参考本人 `tuxianghuaban` 仓库加入 50px 检测边框、原图/局部对比度/高分辨率补扫、精确与召回双阈值、多路位置共识、嵌套文本去重、弱伪字符过滤及全文品牌拼写一致性校正，空白模板仍保持待标注。模板入库和重新识别新增 OCR 校对门禁：上传后素材库立即展示文字框、真实图层名称、原始候选、置信度与多路共识，支持修改、删除、补层、候选替换、拖动缩放和精确坐标输入；校对草稿与原始识别证据分别保存，只有显式确认后模板才会启用并进入内容工作流，确认接口使用版本号避免并发覆盖。样式识别进一步区分文字主色、同一行的多色分段和文字背景底色，不再把米色底条灰化或把浅色文字误当白色背景；识别颜色可在校对界面核对、修正，并由大字报生成器与原生画板前后端一致渲染。

- 封面画板职责进一步收敛为纯编辑组件：打开、恢复草稿和最终渲染只消费封面生成任务已经保存的模板图层快照，不再运行 OCR，也不再用重新识别结果覆盖用户修改。模板的字号、字重、字距、颜色、多色分段、描边、阴影和文字底色直接映射为独立画布图层；前端按需加载与服务端相同的 Noto CJK 字体文件，服务端阴影改为真实高斯模糊，降低预览与最终 PNG 的差异。

- 内容工作流升级为 V3.6，修复生成结果始终没有封面的问题：最终文案审批后恢复视觉规划、封面任务提交、外部生成等待、视觉审核和人工选封面节点，选择结果随内容资产版本保存 `cover_asset_id` 与 `cover_job_id`，结果详情弹窗继续通过鉴权资产接口加载并显示最终封面；前端工作流进度新增“生成并保存封面”阶段。大字报 Worker 与任务创建接口统一接受普通上传素材 `source` 和素材库图片 `library_image`，避免素材库产品图被误报为已不存在。素材文件名和显示名不再进入视觉 Agent 输入，旧 checkpoint 重试时也会由输入契约剥离，避免在内容完成后根据名称错误推断地域冲突；素材有效性、权限与用户确认仍在内容生成前完成。视觉方案的 `mixed` 语义会按真实参考图数量映射供应商模式，单张锁定图片使用图生图，不再错误触发“多图参考至少需要两张参考图”；封面结果提交必须与创建工具的真实返回完全一致，禁止在创建失败后提交虚构 CoverJob。历史坏 checkpoint 若已进入等待节点但任务不存在，重试会清空无效引用并回到封面提交节点重新创建。V3.5 历史任务和 checkpoint 保持只读，已完成且从未生成封面的旧任务需要重新生成。
- SiliconFlow 封面适配区分文生图与图像编辑协议：文生图继续使用 `Qwen/Qwen-Image`，图像编辑通过 `/images/generations` 的 JSON Base64 图片字段调用 `Qwen/Qwen-Image-Edit-2509`，不再误用不存在的 OpenAI `/images/edits` 端点；外部封面失败会向工作流透传已清洗的供应商错误信息，便于直接识别余额、鉴权或模型错误。
- 内容生产阶段一的旧素材库封面模板入口替换为 HyCanvas“小红书模板专区”：图库原图与 HyCanvas 模板声明一起锁入事实简报，封面提交节点按模板可填字段写入新标题和副标题，Worker 通过 `hycanvas` 确定性任务实例化设计稿并服务端渲染 1080×1440 PNG，不再调用 image2。图库原图始终以 `cover` 方式成为锁定的画布最底层背景，模板页面底色会被移除，模板只保留字体、字号、颜色、阴影、位置和装饰等可编辑样式层；模板另有声明图片字段时仍可同时替换对应图片层。纯文字模板不再因收到未声明的 `主图` 字段被 HyCanvas 以 400 拒绝；上游错误同时保留状态和安全错误摘要，不再统一误报为服务或凭据异常。输出封面继续进入视觉审核和人工选择；最终内容版本同时保存 design ID、模板、字段、原图、编辑地址与封面资产快照，刷新后可恢复并进入 HyCanvas 继续编辑。专区内置模板继续使用随前端发布的确定性缩略图；工作区自定义模板没有静态缩略图时，HyCanvas 会从模板设计文件和原工作区素材动态渲染首图，再由 ContentSwarm 使用登录态代理给浏览器，不再显示空白占位。事实简报和 CoverJob 的模板 ID 校验同时接受内置 `xiaohongshu-*` ID 与 HyCanvas 自定义模板 UUID；自定义模板没有声明可填字段时允许原样实例化，并继续使用锁定的图库主图作为背景，避免进入生产时报 422 或上游 400。失败节点重试会按原 CoverJob 模式判断 provider，HyCanvas、画板渲染和其他确定性任务不再错误要求配置 image2，并保留原模型标识与请求快照。

- 优化 V3.5 内容工作流耗时：结构化结果校验成功后同步、异步工具链均在固定回边再次调用模型前结束，并显式声明中间件可跳转到结束节点，避免 LangChain 编图时忽略 `jump_to` 而继续发起模型请求；从业务简报确定性形成可追溯的场景证据，避免无真实缺口时调用调研 Agent；策略 Agent 只读取当前行业和内容类型对应的规则、手法及公式候选。

- 内容工作流的知识库证据展示改为只读取最终标题和正文段落真实引用的 Evidence ID，按知识库分组显示引用片段、文档来源及“标题/正文第 N 段”使用位置；只检索但未采用的知识库内容和非知识库证据不再进入该表。成品及每个内容版本新增引用位置快照，刷新后仍保持相同结果；新检索证据保存可信的知识库、文件和分块来源，历史数据缺失知识库 ID 时明确标记为历史来源，不推测归属。结构化结果工具会在来源校验前统一规范化嵌套 Evidence 模型，避免知识库调研完成后因把 `EvidenceDraftV1` 当作字典读取而中止工作流。

- 内容工作流新增实时执行轨迹，并在任务完成、失败或刷新恢复后保留可审计记录；运行轨迹按 `node_id` 回归左侧对应业务步骤，阶段标题随当前执行进度切换，已经输出的有效内容在整次运行中持续保留，后续步骤继续追加到底部并自动滚动。步骤正文不再呈现 Agent、Skill、工具名称、生命周期事件或原始结构化输入，而是从现有 SSE 事件中筛选并连续输出当前正在解决的问题、知识检索结论、策略依据、标题、正文片段、话题和审核结果；策略依据中的内容类型、创作手法及标题/正文公式编码会使用当前规则包翻译为“编码（中文名称）”，组合编码逐项解释，已附中文名称的内容不会重复。不改变 Agent 原始输出或底层工作流。新增内容使用打字流追加，等待后续输出时在正文末尾显示带旋转图标和动态省略点的“正在思考”状态，替代容易被误认为输入光标的闪烁竖线，流程结束后自动消失；纯技术事件只保留在审计数据中。运行区采用与 Agent 对话一致的大尺寸可滚动内容区和底部输入组件，输入框固定占据布局底部而不遮挡正文，流程完成前保持禁用；页面移除顶部“业务素材 / 内容发布 / 审核交付”阶段步骤条和阶段 2 的重复页头，进入后直接展示当前工作内容。流程完成后左侧继续使用同一个对话式执行框，保留此前的完整内容流并在末尾显示完成状态，输入框原位开放；不再切换为输出规格、层级匹配、知识库汇总或内部节点折叠列表，仅在右侧新增生成结果。右侧生成结果卡片固定保留 3:4 封面栏，无封面时显示占位区域，避免成品内容占满卡片。当前成品的“查看详情”改为在任务页打开双栏弹窗，集中展示封面、完整标题、正文和发布标签，支持分别复制并直接进入审核交付，不再跳转独立结果页；桌面弹窗使用确定高度约束右栏网格，固定标题和发布标签，正文区域可独立滚动且不会将底部标签裁出可视范围，移动端使用弹窗整体滚动。AI 修改只有在 Run 完成且成品存在时开放，只能生成新的标题、正文、话题版本，不会改变工作流、checkpoint、策略、证据、规则或封面；模型上下文和确定性校验统一读取成品保存时的冻结策略与证据快照，不再因任务表未同步策略摘要而误报“内容缺少完整策略快照”。不展示模型隐藏思维链或 JSON 代码，事件预览统一限制字段、条数和长度并过滤敏感配置。结果页不再展示标题、正文公式内部编码，改为呈现公式名称、目标说明和正文结构。修复新建任务尚未加载规则包时，运行叙事标签读取空值导致内容生产页整体空白的问题。运行中的策略选择改为直接展示 `CT + M/S + T + C` 创作公式，并用策略构成表和关键证据表解释每个编码、用途及事实来源；已有结构化策略时不再重复输出整段判断依据，底层工作流与 Agent 输出保持不变。该区域的流式正文、创作公式、策略构成表和关键证据表统一复用 Agent 对话的 Markdown-It/KaTeX 渲染链路，流式补全后自动重渲染，表格在窄屏下可横向滚动。

- 内容工作流升级为简化 V3.5，从 27 个节点收敛为 15 个：SOP1 输入后由策略 Agent 一次选择内容方向、组合手法及一个标题/正文公式，固定规则只负责校验锁定；Agent 可选择任一通过行业、渠道、方向、目标、叙事轴、变量和证据硬约束的组合，固定排序仅作为推荐而不再强制选择第一名；仅在锁定公式存在资料缺口时调用一次调研 Agent；删除公式二次排序、人工选公式、第二轮产品 RAG、标题候选/选题及大纲/正文/润色分段调用，改由内容 Agent 按锁定公式一次生成标题、大纲、正文和话题，之后统一校验、审核和审批。

- 内容策略 Agent 单独使用 `medium` 推理强度，覆盖模型供应商的全局默认值；正文、审核等其他 Agent 继续使用供应商原有推理强度配置，降低策略与公式阶段多次模型调用的等待时间。

- 内容工作流升级为 V3.4：标题候选通过确定性校验后，由标题 Agent 从 `selectable=true` 的候选中自动选择最终标题，不再触发人工标题选择；Agent 只能提交候选 ID 和理由，禁止改写标题文本、公式或 Evidence ID。现阶段从工作流移除视觉规划、封面生成、外部等待、视觉审核、人工选封面及发布打包节点，内容通过最终人工审批后直接保存无封面也合法的内容资产版本；前端同步移除“视觉与交付”分组，但保留阶段一的图库图片与封面模板可选入口，选择结果随简报保存且不阻塞标题和正文生产。V3.3 历史 checkpoint 保持只读，新任务默认使用 V3.4。

- 内容方向决策升级为 V3.3：`select_content_direction` 从人工审批改为内容策略 Agent 自动决策；Agent 只能从价值分析给出的候选方向中唯一选择，不得创建方向、锁定组合组或公式，后续组合组仍由固定规则匹配。V3.2 历史 checkpoint 保持只读，新任务默认使用 V3.3，并在任务页显示“Agent 确定内容方向”。

- 优化 V3 内容任务页的工作流呈现：保留 35 个内部节点及原有固定规则、Agent/Skill/RAG、人工关口和失败回修语义，将其归并为“准备资料、确定策略与公式、补充产品资料、生成标题与正文、质量校验与审批、视觉与交付”6 个业务步骤；默认只展开当前或失败步骤，折叠状态展示完成进度，展开后仍可查看全部内部节点用于审计和定位。

- 完善 OpenAI 兼容模型供应商运行时配置：聊天模型加载器会传递供应商自定义请求头，并支持通过 `extra_json` 启用 Responses API、设置 reasoning effort 及关闭响应存储，确保使用 Responses 协议和专用鉴权头的中转站配置不再只停留在管理页面。

- 素材管理统一收口到“素材库”：下线重复的旧“封面管理”侧栏入口、页面、权限项和 `/api/covers` 管理接口；保留素材库“封面模板”、独立“封面生成”入口及 `/api/content/covers` 生成链路。旧 `content_covers` 数据表不在本次代码变更中执行删除，避免自动清理历史数据。

- 新增账号隔离的“素材库”一级入口，提供素材图片与封面模板两个模块；素材图片使用“图库 → 图片网格”两级结构并支持用户新建、重命名、补充说明和安全删除图库，封面模板使用 3:4 竖版大字报墙并提供可新增、编辑、删除的分类管理。系统首次使用会创建图片与封面的默认分类，只有“未分类”作为兜底项不可删除；删除含素材的图库或分类时会在事务内迁移其归属，不移动或删除 MinIO 文件。分类使用账号隔离的稳定 ID，新上传必须选择当前账号下有效的单一分类，历史未知分类归入未分类，素材库不再读写、搜索或展示标签。上传弹窗支持点击选择或拖拽分批添加图片，前端会忽略重复文件并拦截格式、大小和数量不符合要求的文件。复用 `ContentCoverAsset` 文件事实并新增素材库目录项，所有新素材统一写入私有 `image` 桶的用户隔离路径。封面模板继续复用大字报分析和生成链路；提供只复制不删除旧对象的 dry-run/apply/verify/rollback 迁移脚本，将 `source`、`template`、`mask`、`poster_template` 安全迁入 `image` 桶，其中 mask 仅作为内部资产不单独展示，生成结果等非素材对象保持原位。内容任务阶段一新增图库图片单选和大字报模板可选入口；图库在任务页以文件夹卡片呈现，点击文件夹后通过弹框浏览当前图库图片、单选确认，并在主页面持续显示已选结果。选择结果随草稿恢复并在编译时校验归属、状态和底层文件后写入任务及 V3 运行快照；视觉规划被约束为只能使用该图库图片，选择模板后封面任务固定走对应大字报合成链路。任务页模板列表直接读取素材库的完整分页数据，保证数量、名称、分类、顺序和状态同源；页面激活时立即同步，并在可见期间每 10 秒刷新。待标注或已停用条目仍同步展示并说明原因，但不可用于生产；素材库的新增、重命名、分类、启停和删除会自动反映，失效的草稿选择会安全回退到系统自动生成。仍被内容任务引用的图片和模板禁止删除，避免失败重试或 checkpoint 恢复时素材失效。

- 新增“封面生成”完整闭环：内容生产侧栏新增独立入口，支持九宫格、左右/上下分割、前后对比、卡片式和主图辅图等确定性排版；支持通过 `IMAGE2_BASE_URL`、`IMAGE2_API_KEY`、`IMAGE2_MODEL` 接入中转站，完成文生图、图生图、多图参考、模板复刻、蒙版和异步任务轮询；素材与结果存入 MinIO，任务状态与幂等记录存入 PostgreSQL，进度通过 Redis SSE 返回。生成结果可下载、重试、取消并设为内容资产当前封面，小红书草稿/发布任务会锁定并使用所选封面。参考 RedInk 与 guizang-social-card-skill 的产品思路，因其非宽松许可证未复制源码或模板。真实 `gpt-image-2` 验收覆盖四种输入模式以及 Worker/PostgreSQL/MinIO/浏览器模板复刻闭环，供应商非标准原始尺寸会在 Worker 中归一化为请求画板尺寸。AI 生成进一步改为“模型生成无字底图 + Worker 精确叠加中文标题”，增加标题安全区与乱码/水印负向约束，并使用 CJK 粗体、像素级换行和高对比标题面板保证中文标题准确可读。模板复刻改为严格的双参考全画布生成：模板图仅提供文字层级、贴纸、图案、色块、边框和整体视觉风格，原图作为最终封面的唯一底图与主体来源；客户端通过 `IMAGE2_EDIT_PATH`（默认 `/images/edits`）以 multipart `image[]` 固定按“原图、模板图”顺序提交，避免中转站忽略 JSON 图片字段，并通过正向/负向约束禁止旧底图残留、画中画、卡片套图、额外文字和透明棋盘格。Worker 使用 RapidOCR 定位模板主标题、副标题和标签区域，按原位置、对齐和颜色适配关联内容资产文案；标题按“手动标题、内容产物标题、已选候选标题、任务名称”依次回退，再统一输出不透明 RGB PNG。模板复刻界面将模板图和原图并列拖拽上传，移除重复的底部素材区和容易造成误解的矩形替换区域配置。针对小红书分层海报进一步增加原图安全区布局和确定性图层恢复：原图由上传素材锁定；未关联内容资产时，顶部彩色标签、主标题、副标题与底部固定口号使用模板真实像素迁移，不再人工添加黑影或重画字形；关联内容资产时，从内容产物标题、正文和话题概括主副标题，只替换对应语义槽位并保持固定品牌文案和装饰不变。字形提取覆盖“一”等短横笔画，避免固定口号缺字；最终仍输出无模板旧底图、无白色马赛克的不透明 RGB PNG。封面页右上角新增 image2 账号级全局配置弹框，Base URL 与 API Key 保存后对该账号所有封面任务持续生效且互不串号，环境变量保留为回退；密钥不通过读取接口或任务历史回显。所有 image2 封面请求默认强制最高质量和 PNG 输出，含参考图的请求强制最高输入保真；模板图与原图改为无损 PNG 参考，避免 JPEG 压缩降低上层样式复刻质量。

- 模板复刻升级为版本化 V2 处理链路：`gpt-image-2` 不再发送中转站不支持的 `input_fidelity`，所有图像编辑统一使用 multipart `/images/edits`，原图、模板图与蒙版在提交前归一到同一画布尺寸；新增中转站模型探测和账号级验证状态。模板 OCR 被结构化为可编辑文案槽位，未修改文案直接迁移模板原始字形，关联内容资产时只在对应槽位内概括改写；支持每槽位文字、±2% 位置与 85%–115% 字号微调。最终合成锁定原图非编辑区域，并执行尺寸、SSIM、OCR、旧字、棋盘格和溢出质量门禁，未通过时任务不得显示成功；已带模板标题或水印的“原图”会在预览/生成前提示改用干净原片。新增 20 组黄金样例、渲染性能预算、真实中转站 Worker/MinIO/PostgreSQL E2E、分析缓存和 `CONTENT_COVER_TEMPLATE_REPLICATION_V2` 回滚开关；结果操作按钮移至图片下方，不再遮挡封面。

- 封面生成新增“大字报”模式：在模板复刻右侧提供账号隔离的蒙版素材库，支持最多 100 张 PNG/JPG/WebP 批量拖拽导入、分类、标签检索、状态筛选、分页、重新分析、禁用和删除；透明蒙版自动识别产品区域，不透明画板需人工标注后启用。用户可从素材库图库选择底图并调整 contain/cover、缩放、焦点和位移，后端以 1080×1440 确定性图层合成保留模板文字、Logo 和装饰；关联内容资产时仅概括改写可编辑文字槽。可选 image2 只美化未锁定的背景和融合区域，生成后重新叠加底图主体与模板上层，默认最高质量 PNG。任务接入现有队列、SSE、重试、历史恢复、下载和当前封面流程，并增加尺寸、透明度、溢出和锁定策略质量门禁、60 组 Golden Image、Worker 与真实 API 闭环测试。蒙版软删除后可重新导入同一素材，批量冲突通过事务保存点逐项隔离。受保护的蒙版缩略图和历史底图改为通过鉴权 API 下载 Blob 后显示，不再因原生 `<img>` 缺少 Bearer Token 返回 401；刷新、删除和页面退出时会释放临时 URL。大字报前端进一步重构为“选择模板、选择底图、调整内容、预览生成”四步工作台：导入区域按需展开，筛选空状态可一键恢复且不会丢失当前创作上下文，不透明模板可在画布上实时查看底图替换框，参数支持一键恢复默认，image2 设置折叠为可选高级项，最终操作区集中显示模板、底图和预览就绪状态及不可生成原因。底图选择器提供图库切换、搜索、分页和单选确认；实时 Canvas 按后端同一裁切算法叠加底图与透明 PNG 文字装饰模板，构图参数变化会立即重绘，后端精确预览成功后替换实时参考画布；更换或清除只解除当前引用，不删除素材库资产。
- 修复大字报不透明模板中的文字层错判：双语品牌锁定不再被当作可改写主标题，真实大标题会被自动提升为可编辑槽；未修改文案保持原始空格和像素，旧模板在读取与任务快照阶段自动归一，无需重新导入；跨越产品区域的文字擦除不再绘制白色遮挡块。

- 新增内容生产图片 OCR 闭环：接入 RapidOCR，支持按内容任务上传 JPG/PNG/WebP/BMP/TIFF 图片，保存原图与结构化识别结果（文本块、坐标、置信度、耗时），提供查询、原图受保护访问、人工校对、失败重试接口，并在内容生产台增加“图片识别”入口；同时提供 `get_content_ocr_results` 工具供后续 Skills 稳定读取。OCR 校对界面改为全屏双栏工作台，原图与可编辑校对结果同屏展示。

- 新增小红书账号管理与内容分发：账号、扫码会话和发布记录按 Yuxi 用户严格隔离；内容结果页支持选择已登录账号，一键生成 3:4 封面并保存到小红书草稿箱，或在二次确认后直接发布；分发任务绑定内容版本与不可变快照，通过现有 Worker 内的低并发浏览器通道、账号锁和请求幂等键执行，记录每个账号的结果与受保护截图；发布过程中断时不自动重发，而是提示用户到平台后台核对，版本化 API 镜像推送前会运行 Patchright 浏览器 E2E。

- 优化小红书运营闭环：新增内部 `xhs-browser-gateway`，按账号复用持久化浏览器 Profile，并提供受 Yuxi 用户鉴权保护的远程画面、有限人工接管、心跳和会话状态接口；账号绑定、状态检查和分发统一使用集中网关，不再由旧 Worker 启动第二个 Chromium 争抢同一 Profile。分发 Worker 复用同一账号会话保存草稿或发布，人工接管租约会硬阻断登录、状态检查和分发自动化，空闲浏览器自动回收但保留 Profile；账号管理页新增远程运营浏览器抽屉，明确提示草稿保存在平台远程浏览器而非运营人员本机 Chrome。发布审计新增确认人、确认时间、浏览器会话与证据类型，中断或超时进入禁止自动重试的“结果不确定”状态；补充跨用户拒绝、10 账号并发隔离、部分账号失败隔离、固定 Mock 草稿/发布浏览器 E2E 和真实账号网关打开/截图/关闭冒烟验证。生产发布改为 CI 推送版本号与 Git SHA 镜像，服务器仅执行资源预检、镜像拉取、无构建启动、API/网关双健康检查和失败回滚。
- 稳定 Windows/macOS Docker 开发环境的热重载：API 与 Worker 默认使用 `watchfiles` 轮询模式，避免绑定目录的原生文件监控器因资源耗尽退出后仍由旧子进程返回虚假健康。
- 为集中浏览器网关增加并发容量保护：默认最多同时运行 5 个账号浏览器，生产与开发 Compose 设置 4 GiB 内存和 1 GiB `/dev/shm` 上限；容量已满返回可重试的结构化 429，数据库会话 ID 变化时会关闭并替换陈旧上下文，防止浏览器进程失控拖垮主机。
- 修复集中浏览器会话生命周期的并发竞态：陈旧关闭请求不会再误删同账号刚替换的新会话，空闲回收获得会话锁后会复核活动时间，避免关闭已经恢复操作的浏览器；网关 404 恢复会在账号锁内重新校验账号与权威会话，防止已删除账号被迟到请求重新唤醒。远程浏览器前端为截图、心跳和人工操作增加账号级轮询代次，切换或关闭后丢弃迟到响应并正确释放截图 URL；补充确定性并发回归用例。
- 对齐小红书分发的 Worker、内部网关 HTTP 与账号锁超时窗口，避免封面上传或平台响应较慢时被 45 秒默认 HTTP 超时过早中断；账号锁下限会自动覆盖完整分发窗口并预留释放余量。
- 收敛小红书浏览器异常信息：浏览器启动和页面操作的底层异常只写入受控错误码与用户提示，不再把 Profile 路径等内部运行信息返回给运营人员；新增回归测试覆盖错误信息脱敏。
- 强化生产部署安全门禁：部署前强制校验 JWT、实例标识、PostgreSQL、Neo4j、MinIO 与小红书网关凭据，拒绝缺失、公开默认值或长度不足的配置；要求运营入口使用可验证的 HTTPS 地址，并在部署后同时验证本机 API、内部浏览器网关和公网 TLS 入口。发布 CI 将后端门禁按能力域拆为独立 pytest 进程并补充封面、OCR 集成测试，降低单进程依赖导入导致的内存峰值而不减少覆盖；测试与前端构建完成后会清理隔离依赖容器、Docker 临时层和无关预装工具链，为包含浏览器运行时的 API 镜像保留足够磁盘空间；API、Web 与 Sandbox Provisioner 全部候选镜像成功后才统一提升正式标签，防止部分发布，并从正式标签解析最终 digest 归档。生产部署必须提供该 digest 清单，Compose 直接运行 `仓库@sha256:...`，成功清单随版本归档供精确回滚；仅历史版本允许显式启用标签兼容回滚。一键部署会在上传任何文件前先流式执行远程只读资源预检，并在正式启动前复核同一门禁。
- 加固生产 Nginx 入口：HTTP 请求统一使用 308 跳转 HTTPS，并补充 HSTS、MIME 嗅探保护、同源嵌入和 Referrer Policy；宿主机配置仍要求先运行 `nginx -t` 并由运维显式 reload，部署脚本不自动修改 Nginx。

### 开发记录

- 新增内容发布账号、员工与封面管理：侧栏「账号管理」「员工管理」「封面管理」「配置管理」与「智能体管理」同级；账号支持名称、ID、企业号/个人号、关注数、粉丝数、获赞与收藏、作品数、启停状态的查询与增删改，数据分析入口展示当前统计字段；员工支持员工编码、姓名、登录账号、性别、登录端口、角色、启停状态的查询与增删改，不含手机号码字段；封面按中国风骨/欧洲经典/现代之源分类展示，支持新增、启停和删除，并展示图片名、生成量与创建时间；同一风格下图片名不可重复，支持按图片名模糊查询。开发环境 Vite 增加 `/public` 到 MinIO 的代理，封面等已上传图片可正常展示。「配置管理」为可展开目录，其人设管理、权限配置、内容类型配置、变量配置为配置管理的子页面。权限配置维护角色编码/名称，系统用户角色（普通用户/管理员/超级管理员）与内容角色累加展示并联动：关联人员同时统计员工与系统用户，用户管理的角色筛选与分配来自同一角色表，角色改名同步员工与用户，系统角色不可删除。员工新增/编辑时的角色下拉来自该角色表；关联人员弹窗按姓名、编码模糊查询；「授权」进入角色权限矩阵，按模块勾选能力并保存。内容类型配置维护 NRLX 编码与类型名称，支持按编码/名称模糊查询、启停、编辑和删除。变量配置维护 FWTD 编码、变量名称、服务入口、端口（PC/小程序）与版本（简化版/专业版），服务入口为装修家居、好评笔记，列表按服务入口页签切换；默认预置好评笔记 5 条与装修家居 4 条，支持按编码/变量名称/服务入口模糊查询、启停、编辑和删除。新增员工端获客小程序 `/api/mp/*`：短信/微信登录（微信必须授权手机号，并校验员工管理中该登录账号存在且已启用、开通 app 端口；点「获取验证码」即校验手机号与员工账号，未登记、停用或未开通 app 时不发短信；开发环境短信返回 `debug_code`，生产未配置短信或微信时返回 503）、动态表单、编译事实简报后锁定、quick 模式开跑 V3 工作流、内容列表收藏/复制/删除，以及「我的」简介；小程序只使用该前缀，不复用 PC 内容与密码登录接口。内容管理只列出已生成内容编号的任务，无标题时不展示「未生成标题」占位。装修家居「所在区域」为湖南 14 个地级市，选定地级市后再选区县（如株洲市荷塘区、云龙示范区），好评笔记同样可选该字段且非必填。好评笔记最多上传 3 张照片，装修家居仍为单张封面。外框面积按 8.24 报价表带出基础、木制品、主材价格范围，并在区间内按「下限、下限+0.5、之后取整」自动取值（如 15-18万 取 15 / 15.5 / 16 / 17 / 18）。小程序首页按获客门户原型实现增长指标、能力轮播与四步链路，入口进入内容生成。登录页按原型为「密码登录」：手机号+验证码、红色登录、绿色一键登录；一键登录进入微信授权页（昵称头像，再授权手机号，须与员工管理账号一致），授权后进入登录确认页确认或取消。
- 内容生产完成态继续对齐原型：运行态四个业务步骤改为 Codex 式单步骤状态流，移除大面积蓝色折叠卡和完整内部节点铺陈，区域内始终只显示当前业务步骤、当前内部动作与最新一条可展开运行事件；步骤完成后平滑退出并由下一步原位替换，完整审计内容仍保留。运行状态流下方始终展示 AI 对话输入框，流程完成前保持禁用，完成后沿用现有只修改成品、不干预工作流的能力；完成总结按最终原型移除“准备资料”和“使用的 Skills”两个展示项，底层步骤、Skill 调用和审计数据继续保留；左侧将“输出规格”和“层级匹配”分别移动到“生成标题与正文”前后，并统一为带绿色完成勾的步骤折叠样式；“调用的知识库”沿用相同的绿色完成勾、行高及“标题 + 用途说明”样式，默认展开并支持按标题行折叠，标题、图标和调用数量始终可见。Redis 实时事件过期后，Skills 审计汇总会从已完成 Agent 的不可变运行配置快照恢复。右侧生成结果改为带标题栏的独立白色容器，成品卡片使用浅蓝背景和边框，调整类别、地点、时间、标题、正文与方形封面的排版，底部按“查看详情 / 发布”区分次要和主要操作，并保留“展开全文 / 收起全文”交互。外层卡片和详情弹窗的“发布”统一打开同一个小红书账号选择弹窗；详情弹窗不再展示或跳转“进入审核交付”。发布时加载账号管理数据及当前用户已绑定的小红书账号，在原型式搜索单选表格中选择可用账号后，调用现有分发能力公开发布当前内容版本并实时展示任务结果；停用、未绑定或未登录账号不可选择。


- 产品品牌与门户升级为独立的 ContentFlow 企业内容生产平台：移除用户入口中的博云东方名称、Logo 与粉末冶金行业文案；重做首页价值主张、四大能力入口、内容工作流预览与内容生产 CTA；同步统一登录页、工作台侧栏、聊天空状态、浏览器标题和默认智能体身份，并新增适配浅色/深色模式的 ContentFlow 品牌色与矢量标识。
- 新增面向中小微企业的通用内容生产工作台：提供简化版/专业版动态行业表单和业务素材、创作策略、内容生成、审核交付四阶段交互；将 4 种核心创作手法、场景增强、7 类标题公式、4 类正文公式、组合矩阵、6 个首批行业模板及通用工作流版本结构化保存到 PostgreSQL；新增 `ContentWorkflowAgent`、4 个内置内容 Skills 和规则/业务事实/证据/审核 Tools，由数据库工作流定义装配 LangGraph，使用同一 `ContentBrief` 与 `EvidenceBundle` 完成标题、正文和话题生成；支持标题人工中断与幂等恢复、SSE 节点进度、失败 checkpoint 重试、结果编辑与重审、来源追溯、规则发布回滚、内容版本和生产历史，并补充规则组合、证据审核、工作流中断恢复、Worker 失败恢复及真实 API 集成测试。
- 内容生产工作流升级为 V3.2：保留公式锁定前的通用证据检索，并在唯一标题/正文公式锁定后新增策略驱动的产品资料二次 RAG。固定节点从完整 `StrategySnapshot` 解析产品介绍、价格、案例证明、品牌与爆款结构参考需求，内容调研 Agent 使用独立 `strategy-product-researcher` Skill 在“智能体管理”中配置且当前用户有权访问的知识库内定向检索，内容任务表单不再重复选择知识库；检索生成带来源、风险和公式槽位映射的产品证据，价格、优惠与效果承诺必须人工确认后才能写入新版不可变 EvidenceBundle。标题、大纲、正文和润色节点只消费绑定策略 hash 与证据 hash 的 `ProductEvidencePack`，仍禁止直接检索知识库；确定性校验会阻断未实际引用映射产品资料的候选或正文。爆款样例仅允许 `style_reference/structure_reference_only`，不能作为事实、引用或数字白名单来源。新工作流使用独立版本 ID，历史 V3/V3.1 checkpoint 保持只读。内容方向与关键事实确认界面把内部 Evidence ID 转换为“中文字段名称：证据值”，保留内部追踪 ID 但不再直接向运营人员展示，并将候选方向的引用证据改为独立的中文编号列表，避免长证据堆叠成段。产品资料契约进一步绑定槽位、目标用途和资料类型，阻止案例证据冒充爆款结构参考；结构化结果工具只锁定一次有效提交，校验失败会把明确原因返回 Agent 并允许其在节点预算内修正重提，避免单次错误映射直接终止工作流。标题产品资料槽位进一步按锁定公式区分必填与可选，标题 Agent 显式接收槽位对应的 Evidence ID 和上一轮失败明细；候选全部阻断时自动返回标题生成节点定点重写，达到回修上限后仍会持久化并在页面逐候选展示缺失槽位与修改建议。
- 完成通用内容生产工厂 V3 的 MCV 分层实现：冻结装修行业四层规则来源与 28 组/91 标题引用/48 正文引用/166 公式配对不变量；新增确定性组合匹配、组内唯一标题/正文公式选择、31 节点版本化工作流、六类正式内容 Agent、required Skills 强制激活、一次性结构化结果契约、业务数据与知识库权限、Evidence 冻结、人工审批与受控返工、异步 CoverJob 恢复及 ArtifactVersion 一致性绑定。运行详情可审计每个节点的 Agent 子 Run、Skill 版本、工具、知识库、公式、证据和外部任务；标题/正文 Agent 禁止检索，研究 Agent 受轮次、知识库、片段、工具、步骤、Token 和超时预算约束。旧库升级补齐工作流和行业包 schema 版本字段，Worker 在内容种子初始化前执行幂等内容迁移；正式内容 Agent 使用配置版本增量升级系统种子权限，用户修改过的同名 Agent 不会被自动覆盖；修复行业包 ORM 漏声明 `schema_version` 导致内容初始化接口返回 500 的问题。补充旧 V3 工作流定义的启动时规范升级、Agent 子 Run 标识长度收敛和 Worker 初始化失败落库；内容 Agent 使用精简运行栈并由独立结果守卫强制调用一次性结构化结果工具，Token 预算按累计生成量执行且超限不可被模型重试吞掉；内容价值分析契约和正式 Skill 统一限定一级方向编码为 `CT01～CT07`，禁止临时 `D01` 编码进入固定组合匹配；内容 Agent 图显式声明运行上下文类型，规则、业务数据和知识库工具在首次执行及 checkpoint 恢复时都从受信任运行上下文读取当前用户权限。失败节点恢复会幂等忽略内容完全一致的 Evidence 回放并阻断同 ID 冲突；公式排序从固定匹配快照读取合法标题/正文候选池；标题 Skill 通过正式依赖读取锁定规则，Agent 执行层拒绝节点范围外工具，数字证据索引覆盖字符串及嵌套值，避免合法面积、预算、工期和结果数字被误判为无来源。封面提交 Tool 只接收任务 ID，尺寸、文案、模式、素材与 plan hash 全部从锁定 VisualPlan 读取；创建成功后结果守卫强制下一次调用结构化提交工具，在保留两次工具硬上限的同时避免模型重填参数消耗额度。封面下载支持由管理员显式配置受信任的图片结果 origin，兼容本地代理 DNS 将供应商 CDN 映射到保留地址的环境，同时继续拒绝未授权的内网地址与重定向；从失败的封面等待节点重试时会创建新的 CoverJob、更新 checkpoint 并绑定新的自动恢复 Run，不再复用已失败任务后立即原样失败。内容生产页面将 `external_wait` 展示为无需人工操作的封面生成进度，并在 CoverJob 终态后自动跟进后端创建的续跑 Run，避免停留在旧 Run 误报未支持节点；审核交付阶段通过受保护的封面资产接口展示当前内容版本绑定的生成封面。内容 Agent 的结构化结果工具在业务校验成功后直接结束当前 Agent 图，校验失败则保留修正循环，避免结果已落入收集器后仍多走一步而误触 LangGraph 递归上限。
  内容节点若重复请求读取已在运行前完整注入的必需 Skill，运行时会在不开放任意文件权限的前提下返回继续执行提示并计入工具预算；读取其他未授权路径仍会被硬阻断，避免模型冗余 `read_file` 直接终止工作流。知识检索节点会向 Agent 注入当前已授权知识库的 ID、名称、描述及节点预算，要求按资料需求选择相关库；未通过权限校验的错误 ID 不再占用知识库数量与检索轮次，避免在实际启用库未超限时产生错误的“知识库数量超过节点上限”。检索轮次或不同知识库数量达到预算后，超出的查询会被拒绝并形成审计事件；下一次模型调用只开放结构化结果提交工具，强制 Agent 基于已有结果提交、将证据缺口写入 `unresolved_questions`，不再把预算耗尽升级为整个工作流失败。若模型供应商偶发忽略强制工具选择，结果守卫会在同一图步骤内追加明确纠正指令并重试一次；连续两次不调用才以明确错误停止，避免普通文本被误当作已完成节点。研究节点的 Token 预算同步覆盖完整检索轮次与最终结构化提交；结果提交会移除历史工具调用脚手架，避免兼容模型重复旧查询触发递归上限。新证据若复用已冻结 Evidence ID 会在契约层直接拒绝，历史存量回放则始终以冻结事实为准。封面选择节点通过受保护接口加载真实候选图并释放临时 URL，运营人员可在人工确认前直接预览封面而不是只看资产编号。
  内容 Agent 的业务工具调用预算不再占用唯一的结构化结果提交调用：业务工具达到上限后，下一轮只开放 `submit_content_node_result`，并为该必需提交保留独立额度，避免语义审核并行检索恰好耗尽预算后无法交付审核结果。
- 内容生产切换为 V3 单轨：删除 V2 工作流实现、生产接口和灰度开关，新任务固定锁定 schema 3 的规则、行业包、工作流定义 hash 与运行快照；策略与公式选择全部在 31 节点 V3 工作流中完成，并按节点调用正式 Agent、Skill、知识库与工具。旧 V1/V2 任务、Run、快照和资产仅保留查询与审计，不再允许启动、恢复、重试或修改；前端生产台和规则编辑器同步改为 V3 结构。
- 建立通用 Industry Pack 协议与评测门禁：现有餐饮、教育、美业、零售、专业服务和装修 6 个行业均迁移为独立 V3 Pack，声明 CT01～CT07 别名、行业变量映射、组合候选池、词库、知识范围、证据/合规、Persona、视觉政策、黄金样本、反例、来源和回滚目标；装修保留 28 个用户原版组合组，其他行业使用各自的平台方向组，不复用装修事实。管理端支持 schema/引用/覆盖率离线评测以及 `draft → validated → canary → published → deprecated` 晋级和回滚；发布前必须提交覆盖 CT01～CT07 的真实已完成 Run，并通过规则、证据、引用、审核、Agent/Skill、耗时、Token、费用和封面的全链路回归门禁，交叉组和多公式违规率必须为 0。
- 产品品牌与门户升级为独立的 ContentFlow 企业内容生产平台：移除用户入口中的博云东方名称、Logo 与粉末冶金行业文案；重做首页价值主张、四大能力入口、内容工作流预览与内容生产 CTA；首页进一步升级为行业增长获客门户，以增长率、获客成本和有效线索为核心，增加可轮播的核验数据看板、增长工作流与行业场景入口；同步统一登录页、工作台侧栏、聊天空状态、浏览器标题和默认智能体身份，并新增适配浅色/深色模式的 ContentFlow 品牌色与矢量标识。
- 新增面向中小微企业的通用内容生产工作台：提供简化版/专业版动态行业表单和业务素材、创作策略、内容生成、审核交付四阶段交互；将 4 种核心创作手法、场景增强、7 类标题公式、4 类正文公式、组合矩阵、6 个首批行业模板及通用工作流版本结构化保存到 PostgreSQL；新增 `ContentWorkflowAgent`、4 个内置内容 Skills 和规则/业务事实/证据/审核 Tools，由数据库工作流定义装配 LangGraph，使用同一 `ContentBrief` 与 `EvidenceBundle` 完成标题、正文和话题生成；支持标题人工中断与幂等恢复、SSE 节点进度、失败 checkpoint 重试、结果编辑与重审、来源追溯、规则发布回滚、内容版本和生产历史，并补充规则组合、证据审核、工作流中断恢复、Worker 失败恢复及真实 API 集成测试。自动匹配会跳过缺少输入变量的标题公式，阻断组合不可提交；正文生成使用证据数字白名单并对越界数字自动重写一次；版本记录区分系统默认模型与人工编辑来源；内容 Run 的请求 ID 在 HTTP 部署下使用兼容回退方案，避免浏览器缺少 `crypto.randomUUID()` 时无法启动、恢复或重试工作流。
- 创作规则库新增运营编辑工作台：已发布版本保持只读，超级管理员可基于线上版本创建草稿，维护创作手法、标题公式、正文公式和组合矩阵的新增、编辑、删除、启停与排序；删除规则时同步清理草稿引用，整包事务保存并在发布前校验工作流依赖、公式兼容关系和四类内容目标覆盖，支持未保存提醒及放弃草稿。
- 新增内置 `humanizer-zh` Skill：参考 Humanizer-zh 的中文写作模式，减少模板化结构、宣传腔、空泛结论和机械连接词；改写时强制保留事实、数字、引用、立场与指定语气，不以规避检测器为目标。
- 修复内容生产工作台在公网 HTTP 等非安全上下文中直接调用 `crypto.randomUUID()` 导致无法开始生成、恢复人工节点或重试节点的问题；缺少原生 UUID API 时改用客户端请求 ID 回退方案，保持运行请求幂等。
- 内容生产侧栏新增“最近生成”列表，生成完成的内容会按更新时间展示并可直接进入独立结果详情页；详情页集中展示标题、正文、话题、质量审核、来源证据、策略规则和历史版本，并提供返回任务继续编辑的入口；同步将生产 Web 端口固定为 README 约定的 `8088`，避免与服务器现有 80 端口代理冲突；生产发布保留上一版哈希静态分包，`/assets/` 缺失资源严格返回 404，入口 HTML 禁止缓存，避免已打开页面在发布后动态加载旧分包失败。
- 优化知识库列表与详情页进入速度：知识库详情主分包从约 1.88 MB 拆分至约 46 KB，知识图谱、知识导图、RAG 评估、上传和编辑功能改为按需加载；列表页提前预加载详情外壳，并合并并发的知识库列表请求，避免一次进入重复请求两次数据库列表；检索参数改为进入“检索测试”后再加载，多个检索组件共享同一请求。
- 博云东方品牌色全局统一：新增 `brand-boyun.css` / `brand-boyun.dark.css` 覆盖 `--main-*` 主色阶与 Ant Design `colorPrimary`，登录页、仪表盘及所有内页自动继承官网企业蓝；全局顶栏品牌渐变条；首页与登录页样式收敛至全局变量；系统标题更新为「博云东方 AI粉末冶金智汇中心」；智能体聊天空状态默认展示 10 条粉末冶金领域示例问题；首页四宫格功能区背景使用企业园区全景图并叠加 40% 透明度渐变蒙层，卡片浮于上方。
- 修复 Agent 上下文摘要阶段沙盒写入失败导致整轮对话中断的问题：summary 中间件在 `large_tool_results` 写入超时或失败时改为记录 warning 并继续摘要，不再抛出 `Failed to write tool result` 中断流式回复。
- 博云东方登录页改版为左右分栏布局：左侧白卡登录表单，右侧功能模块展示与品牌文案，风格对齐企业 AI 知识库登录页参考设计。
- 优化思维导图构建接口设计，支持增量构建和更新：新增 GET /mindmap/diff 接口检测文件变更，POST /mindmap/generate 新增 incremental 参数支持增量更新；纯删除场景无需 AI 调用（递归树手术），新增文件时 AI 整合进现有分类结构；前端导图 Tab 新增"增量更新"按钮和变更数量 badge
- 优化文档结构与智能体运行说明：项目简介去除对 LangGraph 具体版本的强调；中间件文档按当前内置 Agent 链路重写，补充知识库工具、Skills 激活、附件/文件系统、子智能体 task、Summary 上下文压缩与工具结果卸载机制；知识库文档补充知识导图与示例问题生成机制；Langfuse 集成文档从“智能体开发”移动到“高级配置”分组。
- 移除知识库普通上传接口遗留的 `allow_jsonl` 参数，上传类型判断统一依赖 `SUPPORTED_FILE_EXTENSIONS`；评估数据集 JSONL 继续通过独立评估接口上传。
- 修复 Dependabot esbuild 告警：web 与 docs 统一锁定 `esbuild@0.28.1`，docs 同步升级 Vite/Vue 插件 override 并固定 pnpm 版本，避免旧锁文件继续解析到存在漏洞的 esbuild 版本。
- 修复添加/编辑 MCP 弹窗中环境变量无法新增的问题：环境变量编辑器存在 rows -> object -> rows 的双向同步回环，`modelValue` 变化时会完全根据已有 key 重建行，导致只填了 key 的行（含刚点击「添加变量」生成的空行）被过滤掉而无法新增；现在仅当传入值与组件自身 emit 的内容不一致时才重建行，避免回声覆盖未填 key 的行。
- 修复模型与知识库后端导入循环：`yuxi.models` 改为惰性导出模型选择函数，知识库可见范围和知识库工具延迟读取全局 `knowledge_base` 实例，避免单测、热重载或轻量导入知识库包时因模块尚未完成初始化而失败。
- 修复知识库创建权限持久化一致性：创建知识库时由 Manager 归一化 `share_config/created_by` 后作为受控记录字段随首次知识库元数据插入写入数据库，避免先插入基础记录再二次更新权限字段产生短暂不一致。
- 修复 HTML 预览 iframe 高度问题：侧边预览模式改为 `height: 100%` 适应父容器，避免底部内容裁切；全屏预览模式移除 `min-height: calc(80vh - 40px)`，避免短内容下方白边；iframe 设为 `display: block` 消除行内基线间隙导致的底部白边；全屏渲染改用独立 `srcdoc`（不注入 `zoom`）按 100% 显示，侧边预览仍保持 0.75 缩放。
- 对话消息图片支持点击全屏预览：对话中用户上传的图片支持点击放大查看，复用文件预览的全屏蒙层交互（Teleport 蒙层，点击图片/空白处或按 Esc 关闭），不引入额外依赖。
- 新增 Agent token usage 状态快照，在状态面板中作为普通可折叠分组展示完整 `messages`、当前传给 LLM 的 `messages`、system/tools 构成、输入构成堆叠条和上下文窗口占用估算。
- 优化 Agent 上下文压缩：Yuxi 的 DeepAgents summary adapter 在生成 summary 与写入 conversation history 时，不再改写 `AIMessage.tool_calls` 或 provider tool metadata，只逐条替换被摘要掉的旧 `ToolMessage.content`；`summary_keep_messages` 保留窗口原样传给模型，不再额外清洗最近消息；完整工具输出写入 `outputs/large_tool_results`，文件名使用工具名与内容 hash 生成，上下文只保留完整路径和最多 `summary_tool_result_token_limit` tokens 的预览，未触发 summary 的常规模型调用不做额外 ToolMessage 清洗；Summary 阈值判断沿用 DeepAgents/LangChain 默认近似 token counter，并保留其 usage metadata scaling；首次写入 `conversation_history` 前读取旧文件的 sandbox 404 会按 `file_not_found` 处理，不再产生误导性 warning；`present_artifacts` 会拒绝展示 `large_tool_results` 与 `conversation_history` 等工具调用阶段文件。新增管理员可配置项 `summary_keep_messages`、`summary_prompt`、`summary_tool_result_token_limit` 与 `max_execution_steps`，分别控制摘要后保留消息数、摘要提示词、summary 阶段工具结果预览上限和 LangGraph `recursion_limit`。
- 收敛普通聊天模型加载链路：`select_model` 保留旧 `.call()` 调用契约，内部改为通过 LangChain chat model adapter 复用 Agent 侧模型加载器，统一 OpenAI-compatible、Anthropic 与 Gemini 等 provider 的运行时适配；移除旧 `OpenAIBase` wrapper，默认重试策略迁移为 LangChain provider 参数。
- 优化 FastAPI 请求链路并发能力：Milvus 知识库检索中的同步 embedding、向量/BM25/混合检索调用，以及图谱查询中的同步 Milvus/Neo4j 读操作（含连接建立）统一通过有界 `asyncio.to_thread` 在线程中执行，避免阻塞 API 事件循环；并发上限按事件循环懒加载信号量控制，不改变检索默认行为与参数上限。
- 改进 OpenAI 兼容提供商流式工具调用兼容（替代 v0.7.0 的按 provider 禁流式处理）：根因是 LangGraph v3 流式累积对 tool_call 字段“后值覆盖”，SiliconFlow、阿里云百炼等在参数续片里把 `name`/`id` 下发为空字符串覆盖首片真实值。改为 `_ToolCallChunkFixChatOpenAI` 把续片空串 `name`/`id` 归一化为 `None`，对所有 OpenAI 兼容 provider 通用生效且保留流式，移除原 `_NON_STREAMING_TOOL_CALL_PROVIDERS` 名单。

## v0.7.0 (2026-06-13)

### 破坏性变更

- Provider 与模型配置收敛：移除旧版 v1 模型配置与 Ollama 支持，运行时模型统一使用 `provider_id:model_id` 与独立 provider 模块；自定义 provider 实现逻辑从文件移动到数据库，并从 config 文件迁移到 provider 模块。
- 智能体运行时语义收敛：用户可见的 `AgentConfig` 收敛为数据库持久化的一级 `Agent`，内置 Python Agent 改为智能体后端；聊天、运行任务、恢复审批和文件预览均从线程绑定的 Agent 解析运行时上下文，前端只提交 `agent_id`。
- 知识库能力边界收敛：移除 Upload 与 LightRAG 知识库/图谱能力，知识库类型收敛为 Milvus 与只读连接器；知识库 API 统一使用 `/databases/{kb_id}/xxx` 形式，并整合 mindmap / eval 等子接口。
- Agent 资源默认选择与权限过滤：未显式配置工具、知识库、MCP、Skills、子智能体时默认启用当前用户可访问/可用的全部资源，显式选择后按允许列表过滤；Agent 创建前统一完成最终资源权限过滤、知识库 `kb_id` 可见范围派生和 Skill prompt/readable 依赖闭包派生。
- Skill 安装与权限模型收敛：Skill 元数据使用 `source_type/share_config/enabled` 表达来源、生效范围与启用状态；内置 Skill 启动或同步时自动写入数据库并默认全局启用，上传和远程添加统一改为解析草稿后确认安装，不保留旧直接安装兼容路径。
- 历史兼容层精简：移除 sandbox provisioner `local` 后端别名、ask_user_question 单问题旧协议、JWT 历史默认密钥特殊判断、内置 Skill `SKILLS.md` 文件名回退、运行事件数字 seq 兼容和前端旧字段回退。
- 用户身份命名收敛：原业务登录标识统一改为 `uid`，Agent/LangGraph runtime、conversation、agent_run、sandbox 路径和前端用户态均使用字符串 `uid`；`user_id` 仅保留给外部响应中的数值 `users.id` 或真实外键场景。

### 开发记录

- 发布版本号更新至 `0.7.0`，同步 package、Docker 镜像标签与快速开始分支引用。
- 新增内置「深度研究」多智能体：编排器 Agent（`deep-research`，ChatbotAgent 后端）负责澄清、拆解、并行调度子智能体与综合成稿，配套两个子智能体 `research-explorer`（围绕单个子问题多轮检索网页/知识库并返回带引用发现）和 `fact-verifier`（对抗式核验关键论断、标注冲突与置信度）；完整研究方法论沉淀为新增内置 Skill `deep-research`（依赖 `tavily_search`），编排器运行时读取并据此调度。三者随 `lifespan` 启动通过 `AgentRepository.ensure_deep_research_agents` 幂等落库（已存在不覆盖管理员修改）。
- 新增内置 `general-purpose` 通用任务子智能体：使用 `SubAgentBackend` 与空运行配置，作为 `task` 工具的通用委派目标，由启动初始化自动写入数据库。
- 收敛 MCP 创建与编辑入口：前端移除整段配置文本入口和模式切换器，仅保留表单字段提交；后端 MCP 创建/更新请求拒绝额外配置字段，避免绕过表单约束。
- 调整内置 MCP 默认项：移除 `sequentialthinking` 的系统内置同步，启动同步时清理历史系统内置记录，保留用户手动创建的同名 MCP。
- 图片生成能力迁移为 Skill：Qwen-Image 从内置 Python 生成工具迁移到内置 Skill `image-gen`，模型调用与图片下载在 Agent 沙盒中完成，生成结果保存到 outputs 并通过 `present_artifacts` 展示，为多图片生成模型接入复用同一产物展示链路。
- 优化前端头像加载兜底：用户与智能体头像优先展示已配置图片，加载失败后回退到基于 ID 的 DiceBear 默认头像；离线或默认头像不可达时显示名称前两个字和稳定背景色。
- 降低知识库路由与工具模块复杂度：示例问题生成迁移到知识库 utils，文件上传统一 100 MB 限制，URL 预处理入库路径与旧 `content_type=url` 行为收敛，并修复 uid、导出 MIME 与异常透传等路由问题。
- 重构智能体配置语义：用户可见的 `AgentConfig` 收敛为数据库持久化的一级 `Agent`，内置 Python Agent 改为智能体后端；新增 `/api/agent` 管理与运行接口，聊天、运行任务、恢复审批和文件预览均从线程绑定的 Agent 解析运行时上下文，前端只提交 `agent_id`，并在模型配置页新增“智能体”管理页签。
- 删除 Upload 与 LightRAG 图谱/知识库能力：知识库类型收敛为 Milvus 与 Dify，只保留 Milvus 知识库内图谱构建/展示/检索，移除独立 `/graph` 页面和默认上传图谱工具。
- 收敛只读知识源连接器：新增 `ReadOnlyConnectors` 基类，Dify 改为声明自身创建参数与校验规则，新增 Notion Data Source 只读知识库并支持 Search/Find/Open；知识库类型接口返回创建参数 schema，前端新建表单按类型动态渲染非 Milvus 配置并统一保存到 `additional_params`。
- 新增知识库 Chunk 持久化：Milvus 知识库索引/更新流程会将 chunks 双写到 PostgreSQL `knowledge_chunks` 表与 Milvus，文件内容查看优先查询 PostgreSQL，并为位置信息、图谱实体关联、标签和抽取结果预留结构化字段；chunk 入库改为分批 embedding 与分批写入，避免大文件一次性写入触发 gRPC 消息大小限制；入库成功后将单文件 chunk 数与 token 数写入文件元数据，并将知识库级总 chunk 与总 token 汇总保存到 metadata，前端文件管理页展示该统计并支持一键修复历史文件缺失的统计值。
- 完善 Milvus 知识库图谱构建：修复 Chunk 图谱写入返回值、Neo4j 同步写入阻塞事件循环、重复构建任务竞态、图谱查询提前终止、Neo4j 连接复用、LLM 抽取超时重试和前端错误详情展示等问题；图谱构建会将 entity/triple 本体与 chunk 引用写入 PostgreSQL，并为唯一 entity/triple 建立 Milvus 语义索引，单文件删除时同步清理图谱引用和孤儿向量。
- 优化图谱抽取器配置：未配置时在图谱中心展示配置入口，抽取方案收敛为 LLM，前端仅保留“更多拓展中”占位；LLM 抽取器使用固定 Prompt + 自定义 Schema，并支持模型参数与并发队列数；已配置后允许修改参数并提示重置重抽风险。修复上传并入库新文件时旧内存 metadata 覆盖数据库图谱配置的问题。
- 新增 Milvus 图谱检索链路：Query 可召回图谱实体和三元组，结合 Chunk 命中实体构造 seed entity，读取 Neo4j 2-hop 子图后用 igraph 执行 PPR，最终以 Chunk 为产物并通过 RRF 与原 Chunk 召回融合；检索配置改为 dataclass 元数据生成，支持 `depend_on` 控制重排序和图检索参数展示。
- 收紧用户管理部门隔离：普通管理员创建用户时固定归属本部门，用户列表、访问选项、详情、更新和删除接口均限制在本部门范围内。
- 修复用户管理列表超过 100 人时被默认分页截断的问题：前端按 `skip/limit` 分批加载用户，并在用户卡片列表中补充分页渲染。
- 调整 Agent 资源默认选择与运行时上下文：未显式配置工具、知识库、MCP、Skills、子智能体时默认启用当前用户可访问/可用的全部资源，显式选择后按允许列表过滤；Agent 创建前统一完成最终资源权限过滤、知识库 `kb_id` 可见范围派生和 Skill prompt/readable 依赖闭包派生，聊天运行时与文件系统预览复用同一结果。
- 重构 Skills 权限与安装流程：Skill 增加 `source_type/share_config/enabled`，内置 Skill 作为启动同步入库的全局资源，不再保留前端安装/更新状态，支持启停但不允许删除；上传和远程添加统一为解析草稿后确认生效范围，安装 slug 优先读取 `SKILL.md` 的 `slug` 字段并保留 `name` 展示名，压缩包名称不参与 slug 校验；管理端支持编辑生效范围与启停；Agent 运行时按当前用户可访问 Skills 派生 prompt/readable 依赖闭包并限制挂载/激活，Skills prompt 改为模型请求级注入以避免污染 runtime context；主智能体恢复 `install_skill` 工具，允许当前用户安装私有 Skill 并激活当前会话，子智能体配置和运行态均禁用该工具。
- 精简历史兼容层：移除 sandbox provisioner `local` 后端别名、ask_user_question 单问题旧协议、JWT 历史默认密钥特殊判断、内置 Skill `SKILLS.md` 文件名回退、运行事件数字 seq 兼容和前端若干旧字段回退。
- 重构知识库共享权限：`share_config` 改为全局共享、部门共享、指定人可访问三档，部门共享必须包含当前用户部门，指定人可访问必须包含当前用户，并补充权限过滤测试。
- 移除知识库沙盒文件系统映射：不再通过 `/home/gem/kbs` 暴露知识库文件树，Agent 继续使用 `query_kb` 与 `open_kb_document` 访问知识库内容。
- 修复 MinerU 文档解析配置说明：文档处理指南原先指引启动 `openai-server`（30000 端口，仅提供 `/v1/chat/completions`），与解析器实际调用的 `/file_parse` 接口不匹配导致 `mineru_ocr` 不可用；更正为使用项目内置的 `mineru-api` 服务（30001 端口），并补充镜像构建与显存调优说明。
- 规范 Agent 知识库 Search/Find/Open 工具协议：`resource_id` 统一表示知识库 `kb_id`，Search 返回结构化 `resource_id/file_id/chunk` 结果，新增 `find_kb_document` 在已知文件内做关键词或正则定位，Open 默认窗口扩大到 1800 行。
- 收敛知识库分块配置：分块预设仅表达策略选择，通用分块参数统一通过 `chunk_parser_config` 传递；移除 `chunk_size`、`chunk_overlap`、`qa_separator` 等旧 root 字段兼容。
- 收敛知识库文件解析参数：文件级 `processing_params` 统一保存 `ocr_engine` 与 `ocr_engine_config`，解析阶段直接使用该结构并保留分块参数快照。
- 修复知识库文件大小显示为 0 的问题：文件上传时 `file_sizes` 参数未正确传播或历史数据缺失导致 DB 中 `file_size` 为 `None`；新增 `MinIOClient.stat_file/astat_file` 获取文件大小方法，`add_file_record` 在 `size` 缺失时从 MinIO 回补，`_load_metadata` 加载元数据后自动为缺少 `size` 的文件从 MinIO 补全并持久化。
- 优化评估基准自动生成：生成任务支持配置队列并发数，默认 10，范围 1-20。
- 完善模型供应商类型：普通聊天模型运行时新增 Anthropic provider type 适配，并清理不再支持的旧 provider type 入口。
- 重梳理知识库评估存储：评估数据集、题目、评估运行和逐题结果统一入库，JSONL 仅作为导入/导出格式；后端和前端 API 统一使用 dataset/run 语义；评估运行支持用户命名，历史记录按名称展示，综合评分只聚合检索指标。
- 扩展知识库上传来源：添加“从工作区上传”模式，后端将当前用户工作区文件预处理上传到 MinIO，前端沿用现有 `addDocuments` 入库链路提交 MinIO URL、内容哈希和文件大小。
- 重构知识库详情页布局：`DatabaseInfo` 改为顶部详情 header + 左侧功能 tab 侧边栏 + 右侧内容区，Milvus 默认进入文件管理，并将检索测试、知识图谱、知识导图、检索配置、RAG 评估和评估基准统一纳入侧边栏导航；只读连接器保留检索测试与检索配置。
- 整合知识导图接口：移除独立 mindmap router 与前端 API 模块，思维导图生成、查询和文件列表接口统一收敛到知识库 API 下。
- 收敛独立模型配置模块运行时：运行时 chat / embedding / rerank 均统一从 provider 模块与模型缓存读取 `provider_id:model_id`；旧版静态模型配置、v1 slash spec、旧模型列表接口和 Ollama 适配已移除；内置 provider 模板补充 XiaomiMiMo、XiaomiMiMo Token Plan CN 与 Kimi Code（`kimi-for-coding`）。
- 调整智能体模型配置默认值：`BaseContext.model` 默认保持为空，运行时按“请求模型 > 智能体配置模型 > 系统默认模型”解析；子智能体未配置模型时继承主智能体当前运行模型，避免把系统默认模型固化进每个智能体配置。
- 调整智能体配置归属与字段权限：`AgentConfig` 从部门共享改为按 `uid` 隔离，所有登录用户可管理自己的配置；`BaseContext` 支持字段级 `auth` 元数据，后端按用户角色过滤可见与可保存的配置项。
- 新增用户级沙盒环境变量：增加 `agent_envs` 表与 `/api/user/agent-env` 接口，设置面板支持当前用户维护 Agent 沙盒环境变量；创建新沙盒时与全局 `sandbox.env` 合并注入，用户变量优先。
- 收敛用户身份命名：原业务登录标识统一改为 `uid`，Agent/LangGraph runtime、conversation、agent_run、sandbox 路径和前端用户态均使用字符串 `uid`；`user_id` 仅保留给外部响应中的数值 `users.id` 或真实外键场景。
- 工作区知识库分类显示：知识库侧边栏按创建者分组为“我的知识库”和“共享知识库”，自己创建的知识库显示在“我的知识库”下，非自己创建的显示在“共享知识库”下；`knowledge_bases` 表新增 `created_by` 字段记录创建者 uid。
- 工作区文件上传支持多选：`/workspace/upload` 与 Viewer 工作区上传统一使用 `files` 多文件字段，一次最多上传 50 个文件，批量上传失败时清理本次已写入文件。
- 聊天附件新增 MinIO tmp 临时上传、可选 PDF/图片解析、确认后加入线程附件的流程；前端改为弹窗内上传、解析与确认。
- 修复智能体对话上传透明 PNG 后图片失真的问题：多模态图片处理在导出 RGB 前会先按白底合成 alpha 通道，避免透明像素中的隐藏颜色被直接转为可见像素；交付物预览优先按文件头识别 MIME，避免 `.jpg` 文件名包裹 PNG 内容时前端按错误格式加载；Agent run 输入消息会持久化为 `multimodal_image`，刷新历史后仍能显示用户上传图片。
- 优化智能体对话页细节：状态面板隐藏空 section，待办名称限制为 20 个中文汉字以内，模型选择器展示供应商名称，并收紧附件状态标签与文件编辑浮动操作样式；
- 标准化 Agent run/SSE 执行链路：run 创建时持久化输入消息并提交后入队，worker 统一写入 Redis Stream envelope，SSE 输出 `event/data/id`、心跳注释、`Last-Event-ID` 回放和终止 `end` 事件；前端强制使用 run API 并支持 ask_user_question 中断后以 resume run 恢复；事件 envelope 构造收敛到统一 helper，前端优先使用 envelope 一级 `thread_id` 路由。
- Agent run SSE 新增 `verbose=false` 精简模式：默认仍返回完整事件载荷；精简模式仅在 SSE 输出前重建最小 payload，跳过 `metadata` 和空 `yuxi.agent_state`，将同一 data 内的 `request_id` 外提为单个字段，移除 chunk 中重复的 `meta`、`metadata`、`thread_id`、`response`、空 `namespace` 和图片 base64 等调试字段，保留消息增量、工具调用、工具结果、非空 Agent state、终止状态和 SSE 游标，前端订阅默认使用精简模式。
- 修复 SiliconFlow MiniMax 与阿里云百炼工具调用流式兼容：二者的 OpenAI 兼容流经 LangGraph v3 event stream 累积工具调用时会丢失关键字段（MiniMax 在参数增量 chunk 返回空 `function.name`，百炼丢失 `tool_call.id`），空值被写入 checkpoint 后会导致工具执行失败或工具结果无法按 `tool_call_id` 关联、工具状态永远停留在“进行中”；这两类提供商默认对工具调用禁用流式模型响应（正文回答仍流式），保留 LangGraph v3 运行事件并拿到完整 tool_call。该缺陷属 LangChain v3 流式协议上游问题（参见 langchain#37420、langchainjs#10937、langgraphjs#2496），截至 langchain-core 1.4.4 仍未修复，待上游修复后可移除对应提供商的禁流式处理。
- 收敛后端模块边界：文档解析从 `plugins.parser` 移动到 `knowledge.parser`，内容审查从 `plugins.guard` 移动到 `services.guard`。
- 收敛文件服务边界：文件预览判断抽为独立服务，Viewer 文件系统的 workspace 分支复用用户 workspace 服务，线程运行时上下文解析从泛化 `filesystem_service` 拆出为 agent runtime helper。
- 升级 DeepAgents 到 0.6.7 并适配新版文件系统协议：SubAgentMiddleware 改为显式 subagent spec，Skills prompt 补齐新版占位符；sandbox/skills backend 复用新版 `ReadResult`、`GlobResult`、`GrepResult` 等协议类型，文件权限在 backend 层明确区分 skills、uploads、outputs 与 workspace，保留最小 `CustomCompositeBackend` 以避免非 route glob 误扫其他 route；Agent 上下文压缩改为复用 DeepAgents SummarizationMiddleware，历史摘要与大工具结果统一 offload 到 outputs。
- 优化聊天输入 @ 文件提及：未创建 Thread 时可搜索用户 workspace，创建 Thread 后按当前对话文件优先、workspace 兜底的来源顺序搜索，并拆分 workspace/thread 缓存避免假 thread 与跨用户缓存污染；输入框与用户消息支持将 raw mention 渲染为带类型图标的引用单元，文件仅显示文件名且保留原始沙盒路径文本。
- 重构子智能体为 Agent-backed 形态：移除旧 `subagents` 表与 `/api/system/subagents` 管理链路，子智能体改为 `agents.is_subagent=true` 且使用 `SubAgentBackend`，创建/编辑统一走 Agent 管理入口；内置后端收敛为 `ChatbotAgent` 与 `SubAgentBackend`，Context 分为 `BaseContext`、`ChatBotContext` 与 `SubAgentContext`；主 Agent 通过 Yuxi task middleware 启动真实子 Agent graph，子智能体不再嵌套调用子智能体。沙盒挂载同步拆分为 child checkpoint thread、父对话 uploads/outputs、用户级 workspace 与子 Agent skills scope；主线程状态记录 `subagent_runs` 并在前端 task 工具中展示子智能体名称、执行状态、child thread 和产物，task 工具结果会暴露 child thread ID 且支持传回 `thread_id` 继续既有子智能体线程；子智能体执行复用 `agent_runs(run_type=subagent)` 记录父 run、child thread 与状态，child thread state 查询以 `agent_runs` 关系为准，不再解析 thread ID 反推父线程；真实流式 E2E 覆盖子智能体输出文件可由父线程文件/Viewer API 读取。流式链路参考 DeepAgents event streaming，后端将 LangGraph v3 raw event 归一化为 Yuxi semantic stream event，按父/子线程归属隔离 run SSE chunk，并支持通过 child thread state 拉取子智能体中间过程。
- 修正评估综合得分计算：`overall_score` 改为有答案准确率时取各题准确率平均，否则取各题 `recall@10` 平均，不再把 recall/f1/各 k 检索指标混合平均；历史已存运行不回填。
- 清理无效鉴权中间件：移除启动时未实际校验令牌的 `AuthMiddleware` 和公开路径残留判断，后端认证边界明确收敛到路由依赖；`/api/auth/me` 改为强制登录并补充未登录访问返回 401 的集成测试。

## v0.6.2 (2026-05-22)

### 新增

- 新增个人工作区预览与管理：提供独立于对话 thread 的用户级 workspace API，并增加“工作区”页面，用于浏览、预览、编辑、上传、下载、删除个人 workspace 文件；默认创建 `agents/AGENTS.md`，并在 Agent 执行时将其内容追加到系统提示词。
- 新增独立模型配置模块：增加 `model_providers` 表、独立管理接口和“模型配置”页面，支持 provider 基础信息、远端候选模型、enabled models 配置和手动添加模型能力。
- 新增远程 Skill 批量安装能力：后端新增 `install_remote_skills_batch()` 与 `POST /remote/install-batch`，前端补充批处理安装 API 和 UI 逻辑。

### 优化

- 下放扩展管理权限：普通管理员现在可进入扩展管理并完整管理 Tools、MCP、SubAgent、Skills；同步放开 Skill 管理接口权限并补充权限测试。
- 调整 Agent 知识库默认选择：未显式配置知识库时默认启用当前用户可访问的全部知识库，显式保存空列表仍表示不启用知识库。
- 优化评估基准自动生成：仅支持 commonrag/Milvus 知识库，默认参考 chunks 数量改为 1；多 chunk 场景复用知识库向量检索选择相似 chunks，不再对全量 chunks 重新计算 embedding。
- 优化 Agent 输入框文件 mention：用户级 workspace 文件候选改为从独立 workspace API 递归加载，不再依赖 active thread；插入时仍转换为 `/home/gem/user-data/workspace/` 沙盒虚拟路径。
- 调整知识库思维导图后端结构：将思维导图路由文件重命名为知识库语义更明确的 router，并把文件列表整理、提示词构建、AI JSON 解析等纯逻辑下沉到知识库 utils。
- 收敛知识库评估后端结构：将评估指标、单题评估、答案生成提示词和自动基准生成算法下沉到 `knowledge/eval`，`EvaluationService` 保留任务、文件和持久化编排职责。
- 扩展管理界面交互逻辑重构：MCP / Subagents / Skills 从“左侧边栏 + 右侧详情面板”调整为“卡片式网格布局 + 路由跳转二级页面”，工具标签页改为卡片网格布局 + 弹窗详情。
- 统一卡片样式：`ExtensionCard` 新增 `tags` prop 并复用于知识库列表页，知识库列表改用 `ExtensionCard` + `ExtensionCardGrid` 替代原有自定义卡片。
- 调整应用主导航：`AppLayout` 升级为默认展开的侧边栏，保留折叠态图标导航，并统一导航项、任务中心、GitHub、用户信息的图标与文字对齐。
- 合并智能体对话导航：移除 `AgentChatComponent` 内部聊天侧边栏，将新建对话入口和对话历史移动到 `AppLayout` 主侧边栏，并通过共享线程 store 统一管理。
- 统一前端 Markdown 预览渲染：新增共享 `MarkdownPreview` 组件与 `markdown_preview` 渲染工具，替换 Agent 消息、文件预览、知识库 chunk、任务工具结果、聊天导出等场景中的旧预览实现。

### 修复

- 修复聊天中普通用户 `@` 提及出不来技能和 MCP 列表的问题：放宽技能列表与 MCP 服务器列表读取接口至已登录用户，并对普通用户请求的 MCP 列表进行敏感连接参数脱敏。
- 修复知识库文档入库状态回退：当已解析文件缺失 `markdown_file` 解析产物时，索引流程会将文件状态恢复为未解析，便于重新解析。
- 修复附件上传后未立即刷新 mention 候选的问题。
- 加固 JWT 鉴权安全：移除历史默认密钥回退，初始化脚本支持生成并持久化 `JWT_SECRET_KEY` 与 `YUXI_INSTANCE_ID`，签发和验证令牌时校验 `iss/aud`，并拒绝已删除或登录锁定用户继续使用旧令牌访问系统。
- 修复模型配置路由请求模型未接收 `embedding_base_url` / `rerank_base_url` 导致前端已填写仍被后端校验拦截的问题。
- 修复知识库文档处理任务状态不一致问题：文件解析失败时任务中心正确显示"失败"而非"已完成"。

## v0.6.1 (2026-04-24)

### 新增

- 合并知识库导航入口：左侧导航仅保留"知识库"，文档知识库与图知识库在页面 header 中通过同一组轻量切换入口切换
- 抽象页面轻量切换 header：知识库与扩展管理页直接共用 `ViewSwitchHeader`，收敛文档知识库、知识图谱、Tools、MCP、Subagents、Skills 等入口的信息层级
- 调整任务中心交互：入口移动到 GitHub 按钮下方，并将右侧抽屉展示改为居中弹窗
- 将 `yuxi` 从 uv workspace 成员调整为 `backend/package` 下可独立构建的本地 Python 包，backend 通过 path dependency 以已安装包形式发现依赖
- 新增 Skills 远程安装能力：Skills 管理页支持填写 `owner/repo` 或 GitHub URL，后端通过隔离的临时 `HOME` 调用 `npx skills add` 下载指定 skill
- 调整部门删除语义：删除部门时不再要求用户数为 0，而是将部门下用户迁移到默认部门
- 扩展 viewer 工作区文件操作：`/home/gem/user-data/workspace` 支持从文件系统面板新建文件夹和上传文件
- 为历史线程补充前端本地配置变更提示：当已有历史消息的对话中切换 Agent、切换配置或编辑配置项时，插入非持久化的信息提示
- 调整 Worker run 模式下的消息首屏反馈：前端发送消息时先乐观渲染用户消息，再将前端生成的 `request_id` 透传给 `/api/chat/runs` 与服务端 `init` 对账
- 调整聊天首页的智能体切换入口：当智能体数量 `>= 4` 或内容区宽度小于 `380px` 时自动收敛为"当前智能体 + 下拉按钮"形式
- 调整智能体对话中的工具调用展示：连续工具调用默认折叠为"调用了 N 个工具"的轻量摘要
- 调整输入框配置入口与侧边栏头尾交互：输入区配置按钮改为轻量 dropdown 触发器

### 修复

- 修复沙盒 `workspace` 隔离粒度：宿主机目录从共享 `saves/threads/shared/workspace` 收敛为用户级 `saves/threads/shared/<user_id>/workspace`
- 收紧文件系统安全边界：viewer/chat 下载与删除路径统一基于解析后的真实路径做允许目录校验，阻止通过软链接逃逸工作区/线程目录
- 修复 OIDC 原始用户名绑定中的占位用户解析：解析目标用户 ID 时改为从右侧拆分，避免 `sub` 中包含冒号时把已绑定账号误判成冲突账号
- 修复 DOCX 解析中的图片回插顺序：Docling 导出的多个 `<!-- image -->` 占位符现在按文档图片顺序替换
- 修复前端依赖安全告警：通过 `pnpm.overrides` 将传递依赖 `flatted` 锁定到 `3.4.2`、`lodash-es` 锁定到 `4.18.1`
- 修复对话摘要中间件的工具结果卸载链路：摘要触发时改为将大体积 `ToolMessage` 写入当前 agent 可见的 sandbox outputs 路径
- 修复 agents 页对话侧边栏在 `keep-alive` 路由切换后的误关闭问题
- 调整 Milvus 混合检索实现：集合 schema 增加 BM25 稀疏向量字段、BM25 函数和中文 analyzer 配置
- 重构 MCP 运行时配置加载模型：移除 `MCP_SERVERS` 作为运行正确性前提的设计，改为每次直接从数据库读取最新 MCP 配置
- 为知识库检索工具补充 `metadata.filepath` 注入：在 `query_kb` 统一出口基于会话可见知识库构建 `file_id -> /home/gem/kbs/...` 映射并回填 Milvus 检索结果
- 移除知识库沙盒文件系统映射：Agent 不再通过 `/home/gem/kbs` 遍历知识库文件，继续通过 `query_kb` 和 `open_kb_document` 检索与打开文档。

## v0.6.0 (2026-04-01)


### 新增
- 重构后端代码 src -> backend/package/yuxi
- 重构文档解析，统一文档解析体验，并新增 Parser 类
- 新增 LITE 模式启动，启动时不加载知识库、知识图谱相关模块，可以使用 make up-lite 快捷启动
- 新增沙盒环境，详见后续文档更新，统一沙盒虚拟路径前缀默认值为 `/home/gem/user-data`
- 新增基于沙盒的文件系统，前端工作台可以查看文件系统，支持预览（文本、图片、PDF、HTML）、下载文件
- 新增 `present_artifacts` 内置工具：Agent 可将 `/home/gem/user-data/outputs/` 下的结果文件显式写入 LangGraph state 的 `artifacts` 字段，前端支持在输入框顶部以默认折叠的堆叠卡片展示本轮交付物文件，并保持可下载、可预览能力
- 交付物卡片新增“保存到工作区”能力：支持将单个交付物复制到共享目录 `workspace/saved_artifacts/`，并复用现有文件树/预览/mention 体系立即可见
- 新增基于沙盒的知识库只读映射，按“用户可访问知识库 ∩ 当前 Agent 已启用知识库”暴露原始文件与解析后的 Markdown
- 重构附件系统，直接集成在了沙盒文件系统中，附件上传后直接落盘到沙盒挂载目录
- 优化前端流式消息体验：新增通用 `useStreamSmoother` 调度层，统一平滑 Agent runs SSE、普通聊天流与审批恢复流中的 `loading` chunk
- 优化项目文档说明，并添加贡献指南
- 重构前端 Agent 路由结构，体验更加顺畅，切换更加自然（类 chatgpt 体验）
- 新增 API Key 认证功能，支持外部系统通过 API Key 调用系统服务
- 新增 subagents 的支持，支持在 web 中添加 subagents，以及两个内置的子智能体
- 新增内置Skills reporter，并移除内置 Agent reporter，数据库报表将由 Skills 完成
- 新增内置 Skills `deep-reporter`，用于指导生成科研报告、行业调研和其他深度分析类长报告
- 重构内置 Skills/MCP/Subagents 安装/添加/移除机制：内置 skill 支持按需安装、基于 `version + content_hash` 的更新提示与覆盖确认，不再使用服务器级开关切换
- 新增知识库 PDF、图片的预览功能
- 重构后端测试目录结构：按 `unit / integration / e2e` 分层迁移现有测试，拆分全局 `conftest.py`，统一测试入口为 `uv run --group test pytest`，并新增独立测试规范文档 `docs/develop-guides/testing-guidelines.md`
- 新增工具元数据 `config_guide` 字段：后端工具列表接口现在可返回“给人看的配置说明”，前端工具详情页会展示该说明，用于提示工具使用前需要配置的环境变量或入口；首批为 MySQL 工具和 `Qwen-Image` 补充了配置指引
- 补充 Langfuse 集成方案文档：明确采用“云端优先、先 tracing 后 feedback”的接入路径，并约定 Yuxi 的 `user/thread` 到 Langfuse `user_id/session_id` 的映射关系
- 新增面向用户的 Langfuse 集成文档：在“高级配置”分组中说明 Langfuse 的定位、能力、配置方式与查看路径，并与当前 `LANGFUSE_BASE_URL` 配置保持一致

<!-- 添加到这里 -->

### 修复

- 调整聊天首页的智能体切换入口：在无历史对话时，智能体数量 `<= 3` 且 `chat-main` 宽度不小于 `380px` 时继续使用横向 segmented；当智能体数量 `>= 4` 或内容区宽度小于 `380px` 时自动收敛为“当前智能体 + 下拉按钮”形式，避免多智能体或窄屏场景下入口被截断
- 发布前一致性修复：统一 0.6.0 版本号（backend/package/web）、更新 dev/prod 镜像标签语义（`0.6.0.dev` / `0.6.0`），并为 `/api/system/health` 补充 `version` 字段，提升部署可观测性与发版追溯能力
- 收敛“状态工作台”自动弹出规则：前端不再因为共享 `workspace` 或文件系统天然存在内容而默认展开，改为仅在 `/home/gem/user-data/uploads` 或 `/home/gem/user-data/outputs` 下检测到实际文件时自动弹出；手动打开、关闭、刷新和伸缩交互保持不变
- 调整智能体 todo 展示语义：待办状态不再作为 `capabilities` 前端开关，而是直接根据运行态 `agent_state.todos` 渲染；同时将 todo 入口从 Agent Panel 移到输入框内的轻量浮层，并让右侧“状态工作台”收敛为文件系统视图，输入框按钮文案同步由“状态”调整为“文件”
- 优化 Agent 输入框 mention 行为：在保留附件 mention 的同时，将共享 `workspace` 文件纳入候选范围；并将 `@` 空查询时的候选列表改为空，仅在继续输入后再执行筛选，避免工作区文件过多时直接铺满下拉面板
- 为前端工作台文件树补齐文件删除能力：`/api/viewer/filesystem/file` 新增删除接口，`AgentPanel` 文件节点新增删除按钮与确认交互，删除后会同步刷新树与预览状态
- 扩展 Agent Panel 状态工作台删除能力：继续复用 `DELETE /api/viewer/filesystem/file`，在保持接口不变的前提下支持删除文件夹；空目录与非空目录现在都会递归删除，`workspace` 下目录也可直接清理，前端目录节点同步新增删除入口与对应确认文案
- 调整前端工作台文件预览交互：恢复默认侧边/弹窗预览，并新增显式“全屏预览”入口；全屏模式下由预览内容直接覆盖整页，仅保留右上角悬浮关闭按钮；同时修复 HTML 文件首次在弹窗中预览偶现白屏的问题，改为在内容更新后强制重建 `iframe`
- 统一 Agent Panel 文件预览与消息区交付物预览组件：两处改为复用同一套 `AgentFilePreview` 预览实现，并为交付物预览补齐与工作台一致的“全屏预览”入口
- 修复交付物卡片展开后的长列表展示：当单轮交付物文件超过面板可见高度时，卡片内容区改为显示纵向滚动条，避免超过约 10 项后底部文件与操作按钮被裁切
- 兼容旧版已安装的内置 `reporter` 技能记录：`update_builtin_skill` 现在会识别由 `system` 或 `builtin-system` 管理的历史记录，避免更新时误报“技能 `reporter` 不是内置 skill”
- 调整沙盒 user-data 目录隔离策略：`workspace` 改为共享目录 `saves/threads/shared/workspace`，`uploads/outputs` 继续保持 thread 级隔离；同时更新 thread artifact 权限校验、viewer 文件系统列举逻辑，以及对应的 router/E2E 测试
- 重构聊天接口请求模型：流式与非流式聊天统一使用 `query + agent_config_id` 请求体，并移除路径中的 `agent_id`；同时修复非流式接口实际误走流式执行链路的问题，改为调用 `invoke_messages` 一次性执行，并补充对应测试
- 修复对话线程与 Agent 配置错位的问题：发送消息时将当前 `agent_config_id` 绑定到 thread 的 `extra_metadata`，线程列表接口返回该绑定值，前端切换历史 thread 时会自动恢复对应配置
- 为沙盒与 viewer 文件系统补齐知识库只读映射：新增 `/home/gem/kbs` 命名空间，按“用户可访问知识库 ∩ 当前 Agent 已启用知识库”暴露原始文件与解析后的 Markdown，并补充对应后端与 viewer 路由测试
- 优化 viewer 文件系统目录树加载：根目录与 `/home/gem/user-data` 改为直接读取本地线程挂载目录，不再为只读树视图触发 sandbox 冷启动，并补充对应后端测试
- 修复 `/home/gem/user-data` 根目录文件不可见的问题：根目录现在会同时展示 thread 目录下的真实文件和 `workspace` 入口，不再只保留固定命名空间目录
- 修复前端工具图标与渲染匹配不准确的问题：工具管理列表与工具调用结果统一改为基于工具 `id` 的精确映射，避免模糊匹配导致的误渲染，未命中的工具不再显示默认扳手图标
- 修复 GitHub Pages 文档部署工作流失败：移除 `actions/setup-node@v4` 对不存在 `docs/package-lock.json` 的缓存依赖，并将 `docs` 目录安装命令从 `npm ci` 调整为 `npm install`，避免因未提交锁文件导致 CI 在依赖缓存和安装阶段直接失败
- 修正沙盒 provisioner backend 命名与配置说明：统一对外使用 `docker` / `kubernetes`，保留 `local` 作为兼容别名；同步清理 compose 中未生效的 provisioner 环境变量、补齐 K8s 相关变量注释，并更新沙盒架构文档中的默认模式与 backend 描述
- 修复智能体配置列表接口在“无配置自动创建默认配置”路径下的参数缺失：补齐 `get_or_create_default` 的 `agent_id` 入参，避免 `/api/chat/agent/{agent_id}/configs` 返回 500
- 修复 LightRAG 同库写入并发导致的入库失败：为 `index_file` / `update_content` 增加按知识库维度的串行锁，并补齐 `documents` 接口 `auto_index` 阶段对最新解析状态的回写与回归测试，避免长时间入库任务进行中再次选择同库文件时直接并发写入报错
- 发布内容工作流 V3.1：为 12 个 Agent 节点增加专属输入契约与上游产物装配，拆分 Agent 可见输入和服务端治理快照；公式锁定后生成可校验的不可变 StrategySnapshot，并增加确定性校验硬门禁、人工确认定点回修、审核契约一致性和前后端审批阻断；同步升级内容 Skills，旧 V3 checkpoint 保持只读且不套用新定义

<!-- 添加到这里 -->


---


# 2026-09-04

- HyCanvas 保存封面模板时改为使用编辑器当前画板，避免自动保存延迟导致字体样式回退；可填充文字的 `system` 字体会固化为 `Noto Sans SC`，并在字段协议中自动记录字体、字重、字号、字距、行高和对齐方式。

## v0.5

### 新增

- 优化 OCR 体验并新增对 Deepseek OCR 的支持
- 优化 RAG 检索，支持根据文件 pattern 来检索（Agentic Mode）
- 重构智能体对于“工具变更/模型变更”的处理逻辑，无需导入更复杂的中间件
- 重构知识库的 Agentic 配置逻辑，与 Tools 解耦
- 将工具与知识库解耦，在 context 中就完成解耦，虽然最终都是在 Agent 中的 get_tools 中获取
- 优化chunk逻辑，移除 QA 分割，集成到普通分块中，并优化可视化逻辑
- 重构知识库处理逻辑，分为 上传—解析—入库 三个阶段
- 重构 MCP 相关配置，使用数据库来控制 [#469](https://github.com/xerrors/Yuxi/pull/469)
- 使用 docling 解析 office 文件（docx/xlsx/pptx）
- 优化后端的依赖，减少镜像体积 [#428](https://github.com/xerrors/Yuxi/issues/428)
- 优化 liaghtrag 的知识库调用结果，提供 content/graph/both 多个选项
- 优化数据库查询工具，可通过设计环境变量添加描述，让模型更好的调用
- 优化任务组件，改用 postgresql 存储，并新增删除任务的接口
- 支持更多类型的文档源的导入功能（支持后端配置的白名单的 URL 导入）

### 修复

- 修复文件上传弹窗中 OCR 下拉选项展开时不会自动检查服务状态的问题
- 修复知识图谱上传的向量配置错误，并新增模型选择以及 batch size 选择
- 修复部分场景下获取工具列表报错 [#470](https://github.com/xerrors/Yuxi/pull/470)
- 修改方法备注信息 [#478](https://github.com/xerrors/Yuxi/pull/478)
- 修复多次 human-in-the-loop 的渲染解析问题 [#453](https://github.com/xerrors/Yuxi/issues/453) [#475](https://github.com/xerrors/Yuxi/pull/475)
- 修复沙盒后端接入回归：补齐 composite backend 的 `sandbox_backend` 参数、限制 `/api/sandbox/prepare` 仅允许访问当前用户线程、确保 `release()` 之后的 `destroy()` 会真正停止热池容器，并恢复 docker-compose 的完整模式默认值
- 重构沙盒为 deer-flow 风格的 AIO provider：切换为 thread-local sandbox、统一 `/home/gem/user-data/{workspace,uploads,outputs}` 固定路径、移除公开 `/api/sandbox/*` 生命周期接口，并补充 lite 模式下的 provider 生命周期、filesystem API 与 sandbox 复用/隔离 E2E 验证
- 调整聊天附件存储链路：线程附件改为直接落盘到 `saves/threads/<thread_id>/user-data/uploads`，解析成功后额外生成 `uploads/attachments/*.md`，不再依赖 MinIO 或显式上传到 sandbox
- 修复知识库文件列表包体异常膨胀：上传阶段不再把批次级 `content_hashes` 写入每个文件的 `processing_params`，并从数据库详情列表接口中移除该字段，改为按需读取单文件详情

## v0.4

### 新增
- 新增对于上传附件的智能体中间件，详见[文档](https://xerrors.github.io/Yuxi/advanced/agents-config.html#%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E4%B8%AD%E9%97%B4%E4%BB%B6)
- 新增多模态模型支持（当前仅支持图片），详见[文档](https://xerrors.github.io/Yuxi/advanced/agents-config.html#%E5%A4%9A%E6%A8%A1%E6%80%81%E5%9B%BE%E7%89%87%E6%94%AF%E6%8C%81)
- 新建 DeepAgents 智能体（深度分析智能体），支持 todo，files 等渲染，支持文件的下载。
- 新增基于知识库文件生成思维导图功能（[#335](https://github.com/xerrors/Yuxi/pull/335#issuecomment-3530976425)）
- 新增基于知识库文件生成示例问题功能（[#335](https://github.com/xerrors/Yuxi/pull/335#issuecomment-3530976425)）
- 新增知识库支持文件夹/压缩包上传的功能（[#335](https://github.com/xerrors/Yuxi/pull/335#issuecomment-3530976425)）
- 新增自定义模型支持、新增 dashscope rerank/embeddings 模型的支持
- 新增文档解析的图片支持，已支持 MinerU Officical、Docs、Markdown Zip格式
- 新增暗色模式支持并调整整体 UI（[#343](https://github.com/xerrors/Yuxi/pull/343)）
- 新增知识库评估功能，支持导入评估基准或者自动构建评估基准（目前仅支持Milvus类型知识库）详见[文档](https://xerrors.github.io/Yuxi/intro/evaluation.html)
- 新增同名文件处理逻辑：遇到同名文件则在上传区域提示，是否删除旧文件
- 新增生产环境部署脚本，固定 python 依赖版本，提升部署稳定性
- 优化图谱可视化方式，统一图谱数据结构，统一使用基于 G6 的可视化方式，同时支持上传带属性的图谱文件，详见[文档](https://xerrors.github.io/Yuxi/intro/knowledge-base.html#_1-%E4%BB%A5%E4%B8%89%E5%85%83%E7%BB%84%E5%BD%A2%E5%BC%8F%E5%AF%BC%E5%85%A5)
- 优化 DBManager / ConversationManager，支持异步操作
- 优化 知识库详情页面，更加简洁清晰，增强文件下载功能

### 修复
- 修复 GitHub Actions 的 Ruff CI 在仓库根目录执行 `uv sync` 导致找不到 `backend/pyproject.toml` 的问题，同时统一检查路径为 `backend/package`
- 修复重排序模型实际未生效的问题
- 修复消息中断后消息消失的问题，并改善异常效果
- 修复当前版本如果调用结果为空的时候，工具调用状态会一直处于调用状态，尽管调用是成功的
- 修复检索配置实际未生效的问题
- 修复 sandbox 文件系统 `ls` 在异常输出下触发 `KeyError: 'path'` 的问题，并将工具调用异常降级为错误消息，避免直接中断聊天 stream
- 修复智能体状态面板中文件树仍依赖 `agent_state.files` 的问题，改为通过真实 `/api/filesystem/*` 接口按层懒加载后端可见文件系统，并让输入框下方状态按钮常态化打开工作区视图
- 为工作台新增 viewer-oriented filesystem service 与 `/api/viewer/filesystem/*` 接口，解耦 agent backend 语义，支持真实目录浏览、原始文件读取与下载
- 重写沙盒技术文档，明确 thread-local sandbox、viewer-oriented filesystem service、`/mnt` 命名空间、skills 可见性与当前实现边界，替换过时的 `/api/sandbox/*` 与 user-level 设计描述
- 收紧沙盒遗留代码：修复未注册 `sandbox_router` 中残留的 user/thread 参数错位，改进宿主机挂载路径映射逻辑，并为 remote sandbox provisioner 增加基础 URL 校验与销毁失败日志
- 修复 builtin skill 内容哈希计算对单文件使用 `read_bytes()` 的无上限内存读取问题，改为分块计算并补充回归测试

### 破坏性更新

- 移除 Chroma 的支持，当前版本标记为移除
- 移除模型配置预设的 TogetherAI


## v0.3
### Added
- 添加测试脚本，覆盖最常见的功能（已覆盖API）
- 新建 tasker 模块，用来管理所有的后台任务，UI 上使用侧边栏管理。Tasker 中获取历史任务的时候，仅获取 top100 个 task。
- 优化对文档信息的检索展示（检索结果页、详情页）
- 优化全局配置的管理模型，优化配置管理
- 支持 MinerU 2.5 的解析方法 <Badge type="info" text="0.3.5" />
- 修改现有的智能体Demo，并尽量将默认助手的特性兼容到 LangGraph 的 [`create_agent`](https://docs.langchain.com/oss/python/langchain/agents) 中
- 基于 create_agent 创建 SQL Viewer 智能体 <Badge type="info" text="0.3.5" />
- 优化 MCP 逻辑，支持 common + special 创建方式 <Badge type="info" text="0.3.5" />
- LightRAG 知识库应该可以支持修改 LLM

### Fixed
- 修复本地知识库的 metadata 和 向量数据库中不一致的情况。
- v1 版本的 LangGraph 的工具渲染有问题
- upload 接口会阻塞主进程
- LightRAG 知识库查看不了解析后的文本，偶然出现，未复现
- 智能体的加载状态有问题：（1）智能体加载没有动画；（2）切换对话和加载中，使用同一个loading状态。
- 前端工具调用渲染出现问题
- 当前 ReAct 智能体有消息顺序错乱的 bug，且不会默认调用工具
- 修复文件管理：（1）文件选择的时候会跨数据库；（2）文件校验会算上失败的文件；
# 2026-09-02

- 内容任务新增“原创模式 / 爆款仿写”切换；爆款仿写由内容调研 Agent 选择并冻结唯一爆款结构参考，再由 `viral-structure-rewriter` Skill 按标题钩子、叙事层级、段落节奏、情绪和互动方式完成结构仿写，真实业务事实继续来自知识库证据，匹配失败不静默降级。
- 强化爆款仿写 Skill：生成前强制建立结构映射表，提交前逐项验收开头、节奏、情绪、符号和互动方式，任一项未落实时禁止提交生成结果。
- 补强爆款仿写的 Emoji 植入能力：参考爆款含 Emoji 时，要求在多个功能位置保留真实 Emoji，禁止用普通项目符号或编号替代。
- 新增 `viral-layout-formatter` 爆款排版 Skill：按参考爆款的段落节奏拆分开头钩子、项目数据、改造条目、落地背书和互动收尾，要求最终正文保留真实换行与留白，同时禁止为排版改动业务事实；内容审核 Skill 会阻断被压平成连续大段的爆款仿写正文并触发定点回修。
- 优化爆款 Emoji 分布规则：按参考区分叙事分散型、清单连续型和混合型，保留句中、句末、转折或互动位置；报价、材料、步骤等清单允许连续使用行首 Emoji，避免把所有内容机械处理成统一段首符号。
- 修复内容任务图库原图标记为“可选”但前端仍强制选择的问题；未选图片时仍可保存所选 HyCanvas 模板并进入正文生产。


## 2026-09-08

- 新增资料优先工作流的报价补证版本：参考因报价缺口无法选择时，检索价格库、展示来源与口径供确认、合并证据后复评；标准单价可独立展示报价明细，不要求工程量、小计或与项目总预算对应、加总一致，选择、生成与审核统一执行此口径；已补证的策略锁定失败重试时保留确认事实并重新复评。标准单价不冒充项目成交明细，保留历史工作流定义。
