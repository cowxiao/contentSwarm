# region model_provider
SILICONFLOW_API_KEY=  # 推荐使用硅基流动免费服务 https://cloud.siliconflow.cn/i/Eo5yTHGJ

# image2 封面生成中转站（Base URL 不包含具体接口路径）
IMAGE2_BASE_URL=
IMAGE2_API_KEY=
# 必须填写中转站模型列表中的图片模型 ID（例如 gpt-image-2），不要填写 high 等质量参数
IMAGE2_MODEL=
# IMAGE2_SUBMIT_PATH=/images/generations
# IMAGE2_EDIT_PATH=/images/edits
# SiliconFlow 图像编辑使用同一个 generations 端点和 JSON 图片字段：
# IMAGE2_EDIT_PATH=/images/generations
# IMAGE2_EDIT_MODEL=Qwen/Qwen-Image-Edit-2509
# IMAGE2_EDIT_REQUEST_FORMAT=siliconflow_json
# IMAGE2_STATUS_PATH=/images/generations/{task_id}
# IMAGE2_TIMEOUT_SECONDS=120
# IMAGE2_POLL_INTERVAL_SECONDS=2
# IMAGE2_POLL_TIMEOUT_SECONDS=900
# IMAGE2_MAX_CONCURRENT=1
# IMAGE2_SEND_RESPONSE_FORMAT=false
# CONTENT_COVER_BUCKET=content-covers
# 当家内容生成结果回调（开发：http://mgr.dev.dangjia.com:8001；测试：http://test-mgr.fengjiangit.com:8005；高保真：http://uat-mgr.fengjiangit.com:8014）
# DANGJIA_CALLBACK_BASE_URL=
# DANGJIA_CALLBACK_API_KEY=
# 当家最终封面的永久业务 URL 前缀
# DANGJIA_MEDIA_PUBLIC_BASE_URL=https://content.example.com/api/dangjia/content/media
# 远程 VisioFlow 素材库同步（管理员调用 /api/material-library/remote-sync）
REMOTE_MATERIAL_BASE_URL=http://10.10.10.50:8088
# REMOTE_MATERIAL_TOKEN=
# REMOTE_MATERIAL_USERNAME=
# REMOTE_MATERIAL_PASSWORD=
# REMOTE_MATERIAL_TIMEOUT_SECONDS=60
TAVILY_API_KEY=  # 获取搜索服务的 api key 请访问 https://app.tavily.com/

# JWT 安全配置：首次部署请生成随机值，初始化脚本会在留空时自动生成
YUXI_ENV=development
JWT_SECRET_KEY=
YUXI_INSTANCE_ID=

# 获客小程序微信登录（生产必填；未配置时微信授权登录返回 503）
WECHAT_MP_APPID=
WECHAT_MP_SECRET=

# # 其余可选配置
# OPENAI_API_KEY=
# OPENAI_API_BASE=
# ZHIPUAI_API_KEY=
# DASHSCOPE_API_KEY=
# DEEPSEEK_API_KEY=
# ARK_API_KEY=
# MINIMAX_API_KEY=  # MiniMax 大模型 https://platform.minimaxi.com/
# TOGETHER_API_KEY=
# # endregion model_provider

# # region neo4j
# NEO4J_URI=
# NEO4J_USERNAME=
# NEO4J_PASSWORD=请填写至少16个字符的生产强密码
# # endregion neo4j

# 生产数据服务凭据（生产部署必须显式配置，禁止使用默认值）
# POSTGRES_USER=yuxi
# POSTGRES_PASSWORD=请填写至少16个字符的生产强密码
# POSTGRES_DB=yuxi
# MINIO_ACCESS_KEY=请填写至少8个字符的非默认访问标识
# MINIO_SECRET_KEY=请填写至少16个字符的生产强密钥

# # Servies
# YUXI_SUPER_ADMIN_NAME=
# YUXI_SUPER_ADMIN_PASSWORD=

# Xiaohongshu distribution worker
# XHS_RUNTIME_ROOT=/app/saves/xiaohongshu
# XHS_BROWSER_GATEWAY_URL=http://xhs-browser-gateway:5051
# XHS_GATEWAY_TOKEN=请替换为至少32个字符的部署环境随机长令牌
# XHS_GATEWAY_HEADLESS=true
# XHS_GATEWAY_IDLE_SECONDS=900
# XHS_GATEWAY_REAPER_INTERVAL_SECONDS=30
# XHS_GATEWAY_MAX_SESSIONS=5
# XHS_GATEWAY_MEMORY_LIMIT=4g
# XHS_GATEWAY_SHM_SIZE=1gb
# XHS_BROWSER_CONTROL_LEASE_SECONDS=120
# XHS_BROWSER_OPERATION_LOCK_SECONDS=180
# XHS_BROWSER_OPERATION_BLOCK_SECONDS=3
# XHS_WORKER_MAX_JOBS=1
# XHS_ACCOUNT_LOCK_SECONDS=600
# XHS_DISTRIBUTION_TIMEOUT_SECONDS=360
# XHS_LOGIN_STATE_TTL_SECONDS=300

# Built-in HyCanvas visual module (source: apps/hycanvas). Local Docker Compose
# supplies working defaults automatically. Override every secret in production.
HYCANVAS_BASE_URL=http://hycanvas-app:8005
HYCANVAS_PUBLIC_URL=http://127.0.0.1:8005
HYCANVAS_API_KEY=
HYCANVAS_WORKSPACE_ID=00000000-0000-4000-8000-000000001001
# HYCANVAS_PORT=8005
# HYCANVAS_DB_USER=hycanvas
# HYCANVAS_DB_PASSWORD=请填写至少16个字符的生产强密码
# HYCANVAS_DB_NAME=hycanvas
# HYCANVAS_JWT_SECRET=请填写至少32个字符的生产随机密钥
# CONTENTSWARM_PUBLIC_URL=http://127.0.0.1:5173

# Production immutable-image deployment (never use latest)
# YUXI_VERSION=0.7.2-contentflow
# YUXI_API_IMAGE=ghcr.io/shenwei8899-ctrl/contentswarm-yuxi-api
# YUXI_WEB_IMAGE=ghcr.io/shenwei8899-ctrl/contentswarm-yuxi-web
# YUXI_SANDBOX_PROVISIONER_IMAGE=ghcr.io/shenwei8899-ctrl/contentswarm-yuxi-sandbox-provisioner
# SANDBOX_IMAGE=enterprise-public-cn-beijing.cr.volces.com/vefaas-public/all-in-one-sandbox@sha256:请填写已验证的镜像摘要
# PUBLIC_BASE_URL=https://content.example.com/boyun
# MINERU_IMAGE=your-registry/mineru-vllm
# PADDLEX_IMAGE=your-registry/paddlex
# WEB_HOST_PORT=8090

# Windows/macOS Docker bind mounts: use polling to avoid native watcher exhaustion
# WATCHFILES_FORCE_POLLING=true

# # URL Whitelist (comma-separated domains/IPs, empty to disable URL parsing)
# YUXI_URL_WHITELIST=github.com,docs.example.com,gitlab.example.com,127.0.0.1

# # MinerU
# MINERU_API_KEY=

# Sandbox (deerFlow-style provisioner)
# SANDBOX_PROVIDER=provisioner
# SANDBOX_PROVISIONER_URL=http://sandbox-provisioner:8002
# SANDBOX_VIRTUAL_PATH_PREFIX=/home/gem/user-data
# SANDBOX_EXEC_TIMEOUT_SECONDS=180
# SANDBOX_MAX_OUTPUT_BYTES=262144
# SANDBOX_KEEPALIVE_INTERVAL_SECONDS=30
# SANDBOX_IDLE_TIMEOUT_SECONDS=120
# SANDBOX_IDLE_CHECK_INTERVAL_SECONDS=10
# # sandbox-provisioner backend: memory | docker | kubernetes
# SANDBOX_PROVISIONER_BACKEND=docker

# sandbox-provisioner 通用配置
# SANDBOX_IMAGE=enterprise-public-cn-beijing.cr.volces.com/vefaas-public/all-in-one-sandbox:latest
# SANDBOX_CONTAINER_PORT=8080
# SANDBOX_HEALTH_TIMEOUT_SECONDS=300
# MEMORY_SANDBOX_URL_TEMPLATE=http://agent-sandbox:8000
# SANDBOX_HTTP_PROXY=http://host.docker.internal:7897
# SANDBOX_HTTPS_PROXY=http://host.docker.internal:7897

# Docker backend 专用 (used when SANDBOX_PROVISIONER_BACKEND=docker)
# 注意：网络名称已在 docker-compose.yml 中固定为 yuxi-know_app-network
# SANDBOX_DOCKER_NETWORK=yuxi-know_app-network
# SANDBOX_DOCKER_THREADS_HOST_PATH=
# SANDBOX_DOCKER_SANDBOX_PREFIX=yuxi-sandbox
# SANDBOX_DOCKER_SANDBOX_HOST=host.docker.internal

# Kubernetes backend 专用 (used when SANDBOX_PROVISIONER_BACKEND=kubernetes)
# SANDBOX_K8S_NAMESPACE=yuxi-know
# SANDBOX_NODE_HOST=host.docker.internal
# KUBECONFIG_PATH=/root/.kube/config
# THREAD_PVC=yuxi-thread
# SKILLS_PVC=yuxi-skills  # 当前代码会读取，但 Pod 挂载实际仍只使用 THREAD_PVC
